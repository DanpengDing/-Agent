# 记忆

一块是会话级的短期上下文，主要保证当前这轮和最近几轮对话不断；一块是长期记忆，用来存用户比较稳定的信息，比如位置、设备型号、设备品牌；还有一块是用户偏好，比如用户希望回答简洁一点，或者更偏向什么处理方式。

但我觉得最关键的是我单独做了一层“任务状态记忆”。因为很多 Agent 场景里，真正难的不是记住用户说过什么，而是记住任务做到哪一步了。比如上一次是不是已经查过工具了、失败在哪个工具、是不是正在等人工审批、要不要继续重试，这些如果只靠聊天历史，其实很不稳定。

所以我在项目里把任务状态单独结构化存了下来，像任务类型、任务阶段、最近一次工具调用结果、最近错误、审批 token、重试次数这些都会记。这样用户下一轮如果说“继续”“再试一次”“换个办法”，系统就不会把它当成一个全新的问题，而是先判断是不是要恢复上一个任务，然后把之前的状态重新注入给 orchestrator，让它接着往下执行。

这个能力比较明显的价值有几个：

第一，多轮对话不会越聊越乱；
第二，工具失败后可以从失败点恢复，不用整段重来；
第三，如果中间有人审，审批通过以后也能继续跑原任务，而不是让用户重新描述一遍。

# 解决幻觉

先是 Agent 正常生成候选答案；然后我会从知识库检索结果或者工具结果里，把能用的证据抽出来，统一整理成证据列表；接着再做一个风险分类，判断这个回答是不是高风险，比如维修建议、BIOS、驱动、维修站地址、导航这类，或者答案里出现“必须、立即、更换主板”这种很强的结论词。

如果命中了这些情况，我就不会直接把答案放出去，而是让它先经过一层审查。

这里我会比较诚实地讲，当前项目里这层审查不是一个特别重的第二个大模型裁判，而是一个轻量、可控的 review service。它主要做两件事：

第一，看证据里有没有冲突；
第二，看候选答案和证据片段之间有没有足够的文本重叠和支撑关系。

如果能支撑，就判成 supported；
如果证据不足，就判 unsupported；
如果证据互相打架，就判 conflicting；
如果根本没拿到可审查的证据，就标成 review unavailable。