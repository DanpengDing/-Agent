# 面试问答 - 技术部分

## 一、Java 基础

### 1.1 HashMap 底层原理

**回答**：

> HashMap 底层是**数组 + 链表 + 红黑树**（JDK8+）。
>
> **put 流程**：
> 1. 计算 key 的 hash 值，然后 `(n-1) & hash` 计算下标
> 2. 如果数组位置为空，直接插入
> 3. 如果不为空，遍历链表/JDK8+：先判断 key 是否相等（hash相同且equals），相等则覆盖
> 4. 如果是链表，插入尾部；链表长度 > 8 且数组长度 > 64，转红黑树
> 5. 如果 key 不存在，size++，判断是否扩容
>
> **扩容机制**：默认容量16，加载因子0.75。扩容时新建两倍大小的数组，然后重新 hash 分配。
>
> **线程不安全原因**：
> 1. JDK7 头插法并发扩容可能造成环形链表，死循环
> 2. JDK8 尾插法也可能造成数据覆盖
> 3. size++ 不是原子操作

---

### 1.2 ConcurrentHashMap 如何保证线程安全

---

#### JDK7：分段锁（Segment）

**原理**：数组被分成 16 个 Segment，每个 Segment 内部是一个小的 HashMap，有自己的锁。

```
ConcurrentHashMap
├── Segment[0] ──── 锁0 ──── HashMap[0..n]
├── Segment[1] ──── 锁1 ──── HashMap[0..n]
├── ...
└── Segment[15] ──── 锁15 ──── HashMap[0..n]
```

**问题**：锁粒度还是太大（每个 Segment 一把锁），并发度固定 16。

---

#### JDK8：CAS + Synchronized（不再分段）

**原理**：取消分段锁，用 Node 数组 + CAS + synchronized 实现。

**数据结构**：
```java
// Node 是链表/红黑树的节点
static class Node<K,V> {
    final int hash;
    final K key;
    volatile V val;           // val 用 volatile 保证可见性
    volatile Node<K,V> next;  // next 用 volatile 保证可见性
}

// 核心变量
transient volatile Node<K,V>[] table;  // 主数组
private volatile int sizeCtl;           // 控制扩容（-1表示正在初始化/扩容）
```

---

#### put 流程

```java
public V put(K key, V value) {
    // 1. 计算 hash
    int hash = spread(key.hashCode());

    // 2. 遍历数组
    for (Node<K,V>[] tab = table;;) {
        Node<K,V> f;
        int n, i, fh;

        // 情况1：数组还没初始化
        if (tab == null || (n = tab.length) == 0) {
            // CAS 初始化数组
            tab = initTable();
        }
        // 情况2：数组位置为空
        else if ((f = tabAt(tab, i = (n - 1) & hash)) == null) {
            // CAS 插入新节点（无锁）
            if (casTabAt(tab, i, null, new Node<K,V>(hash, key, value, null))) {
                break;
            }
        }
        // 情况3：数组位置有值（扩容中或已有节点）
        else {
            // synchronized 锁住头节点，保证安全
            synchronized (f) {
                // 链表插入 / 红黑树插入
                if (f.hash == MOVED) {
                    // 正在扩容，帮助扩容
                    tab = helpTransfer(tab, f);
                } else {
                    // 插入链表尾部
                    for (int binCount = 0; ; ++binCount) {
                        if ((f = f.next) == null) {
                            f.next = new Node<K,V>(hash, key, value, null);
                            // 链表过长转红黑树
                            if (binCount >= TREEIFY_THRESHOLD) {
                                treeifyBin(tab, i);
                            }
                            break;
                        }
                    }
                }
            }
        }
    }
}
```

---

#### get 流程（无锁）

```java
public V get(K key) {
    Node<K,V>[] tab; Node<K,V> e, p; int n, eh; K ek;
    int h = spread(key.hashCode());

    // 直接读取，不用加锁
    if ((tab = table) != null && (n = tab.length) > 0 &&
        (e = tabAt(tab, (n - 1) & h)) != null) {
        if ((eh = e.hash) == h) {
            if ((ek = e.key) == key || (ek != null && key.equals(ek)))
                return e.val;
        }
        // 红黑树
        else if (eh < 0) {
            return (p = e.find(h, key)) != null ? p.val : null;
        }
        // 链表
        while ((e = e.next) != null) {
            if (e.hash == h && ((ek = e.key) == key || (ek != null && key.equals(ek))))
                return e.val;
        }
    }
    return null;
}
```

**为什么 get 不用加锁？**
- 因为 Node 的 val 和 next 都是 volatile 修饰
- 保证了可见性，读取一定能读到最新值

---

#### 面试回答

> ConcurrentHashMap JDK7 用分段锁，每个 Segment 一把锁。
>
> JDK8 取消了分段锁，改用 **CAS + synchronized**：
> - Node 的 val 和 next 用 volatile 修饰，保证可见性
> - 数组为空时，用 CAS 初始化
> - put 时，如果数组位置有节点，用 synchronized 锁住头节点，然后链表/红黑树操作
> - get 时不用加锁，因为 volatile 保证可见性
>
> 相比 JDK7，JDK8 锁粒度更细（只锁一个桶），并发度更高。

---

### 1.3 synchronized 和 ReentrantLock 区别

| 对比 | synchronized | ReentrantLock |
|------|-------------|---------------|
| 底层 | JVM 内置关键字 | JDK 层面实现 |
| 锁类型 | 非公平锁 | 可公平/非公平 |
| 等待可中断 | 不可 | `lockInterruptibly()` 可中断 |
| 绑定条件 | 不能绑定多个条件 | `newCondition()` 可绑定多个条件 |
| 释放方式 | 自动释放 | 必须 finally 释放 |

---

### 1.4 ThreadLocal 原理

**回答**：

> ThreadLocal 为每个线程提供独立的变量副本，通过 `ThreadLocalMap` 实现。
>
> **数据结构**：
> - ThreadLocalMap 是 Thread 的成员变量
> - key 是 ThreadLocal 对象（弱引用），value 是要保存的值
>
> **内存泄漏问题**：
> - ThreadLocalMap 的 Entry 继承 WeakReference<ThreadLocal>
> - 如果没有手动 remove，ThreadLocal 被 GC 后，key 变 null，但 value 不会被回收
> - 线程不结束，value 一直存在，造成内存泄漏
> - **解决方案**：用完手动 remove()

---

### 1.5 线程池参数

**回答**：

> 七大参数：
> ```java
> public ThreadPoolExecutor(
>     int corePoolSize,      // 核心线程数
>     int maximumPoolSize,    // 最大线程数
>     long keepAliveTime,    // 空闲线程存活时间
>     TimeUnit unit,         // 时间单位
>     BlockingQueue<Runnable> workQueue,      // 任务队列
>     ThreadFactory threadFactory,           // 线程工厂
>     RejectedExecutionHandler handler)       // 拒绝策略
> ```
>
> **流程**：任务来 → 核心线程处理 → 队列满 → 扩容到最大线程 → 执行拒绝策略

---

### 1.6 AQS 原理

**回答**：

> AQS（AbstractQueuedSynchronizer）是 JUC 的基础，通过**模板方法模式**实现。
>
> **核心结构**：
> - state：资源计数
> - FIFO 队列：等待线程排队
>
> **两种模式**：
> - **独占模式**：如 ReentrantLock，一次只有一个线程获取资源
> - **共享模式**：如 Semaphore、CountDownLatch，多个线程同时获取资源
>
> **核心方法**：
> - `tryAcquire()`：尝试获取资源
> - `tryRelease()`：尝试释放资源
> - `tryAcquireShared()`：共享模式尝试获取
>
> ReentrantLock 依赖 AQS 的 state 表示重入次数。

---

### 1.7 JVM 内存结构

**回答**：

```
线程共享：
- 堆（Heap）：对象实例、数组
- 方法区（Metaspace）：类信息、常量、静态变量（JDK8+ 用 Metaspace 替代 PermGen）

线程私有：
- 虚拟机栈（VM Stack）：方法调用、局部变量
- 本地方法栈（Native Stack）：native 方法
- 程序计数器（PC Register）：当前字节码行号
```

---

### 1.8 什么情况下会触发 Full GC

**回答**：

> 1. **老年代空间不足**：对象年龄熬过 Minor GC 后进入老年代，老年代满触发
> 2. **System.gc()**：调用 System.gc() 会建议 JVM 触发 Full GC，但不保证
> 3. **Metaspace 满**：类加载太多
> 4. **Minor GC 前的检查**：Minor GC 之前，如果老年代剩余空间 < 历次晋升平均值，会提前触发 Full GC

---

### 1.9 G1 和 ZGC 区别

| 对比 | G1 | ZGC |
|------|-----|-----|
| 停顿时间 | 软目标，可指定 | 最大停顿时间不超过 1ms |
| 内存结构 | Region | Region + ZPage |
| 对象分配 | TAMS指针 | 指针着色 coloring |
| 支持的平台 | JDK9+ | JDK11+ |

---

### 1.10 ArrayList 扩容机制

**面试官可能会问：ArrayList 的扩容机制是怎样的？**

> `ArrayList` 底层是 `Object[]` 数组，容量不够时会触发扩容。核心点可以分 5 步说清楚：
>
> 1. **何时扩容**：执行 `add()` 时，如果 `size + 1 > elementData.length`，就会扩容。
> 2. **扩多少**：新容量通常是旧容量的 **1.5 倍**，公式近似：`newCapacity = oldCapacity + (oldCapacity >> 1)`。
> 3. **首次添加的特殊情况**：无参构造创建的 `ArrayList` 初始并不立刻分配 10 个元素空间；第一次 `add` 时才初始化为默认容量（10）。
> 4. **放不下就继续放大**：如果 1.5 倍仍小于本次所需最小容量（`minCapacity`），会直接扩到 `minCapacity`。
> 5. **数据迁移成本**：扩容会创建新数组并把旧数据拷贝过去，属于 O(n) 操作，所以频繁扩容会影响性能。
>
> **实践建议**：
>
> - 已知数据量时，优先 `new ArrayList<>(capacity)` 预分配，减少扩容次数。
> - 读多写少且需要随机访问时，`ArrayList` 更合适；中间频繁插入/删除可考虑 `LinkedList`（但也要结合实际场景评估）。
>
> **一句话总结**：`ArrayList` 通过“容量不足时按 1.5 倍扩容 + 数组复制”实现动态增长，时间换空间，换来连续内存下更高的随机访问效率。

## 二、Spring

### 2.1 IOC 原理

**回答**：

> IOC（控制反转）是 Spring 核心，将对象创建权交给容器。
>
> **流程**：
> 1. 加载配置文件（XML/注解/Java Config）
> 2. 扫描类，找到 `@Component` 等注解
> 3. 创建 BeanDefinition（包含 class、scope、lazy-init 等）
> 4. BeanFactoryPostProcessor 修改 BeanDefinition
> 5. 实例化 Bean（反射）
> 6. 依赖注入（@Autowired、@Value）
> 7. 生命周期回调（InitializingBean、@PostConstruct）
> 8. 注册销毁回调（DisposableBean、@PreDestroy）

---

### 2.2 AOP 原理

**回答**：

> AOP（面向切面编程）将重复的逻辑抽取出来，统一处理。
>
> **核心概念**：
> - JoinPoint：连接点，所有可拦截的方法
> - PointCut：切点，真正被拦截的方法
> - Advice：通知，增强的逻辑（前置、后置、环绕等）
> - Aspect：切面 = PointCut + Advice
> - Weaving：织入，把通知织入目标方法
>
> **JDK 动态代理**：目标类实现接口时用，Proxy.newProxyInstance()
> **CGLIB 代理**：目标类没实现接口时用，继承目标类生成子类

---

### 2.3 Spring 事务隔离级别

**回答**：

| 级别 | 说明 |
|------|------|
| DEFAULT | 使用数据库默认 |
| READ_UNCOMMITTED | 读未提交（脏读） |
| READ_COMMITTED | 读已提交（不可重复读） |
| REPEATABLE_READ | 可重复读（MySQL默认） |
| SERIALIZABLE | 串行化 |

---

### 2.4 Spring 事务传播行为

**回答**：

| 传播行为 | 说明 |
|----------|------|
| REQUIRED | 默认，有事务加入，没有创建新事务 |
| REQUIRES_NEW | 每次创建新事务，挂起当前事务 |
| SUPPORTS | 支持当前事务，没有就不创建 |
| MANDATORY | 必须有事务，没有抛异常 |
| NOT_SUPPORTED | 不支持事务，挂起当前事务 |
| NEVER | 不能有事务，有抛异常 |
| NESTED | 嵌套事务，有事务则在嵌套事务执行，无则创建新事务 |

---

### 2.5 Spring/SpringBoot 启动流程

**回答**：

> SpringBoot 启动流程核心是**应用上下文创建 + Bean 加载**。
>
> **启动流程**：
> ```
> SpringApplication.run()
>     ↓
> 1. 创建 Bootstrap Context（引导上下文）
>     ↓
> 2. 配置 Headless（无头模式）
>     ↓
> 3. 加载 META-INF/spring.factories（SpringFactoriesLoader）
>     ↓
> 4. 创建并启动 SpringApplicationRunListeners
>     ↓
> 5. 准备 Environment（application.yml/properties）
>     ↓
> 6. 打印 Banner（那个 logo）
>     ↓
> 7. 创建 ApplicationContext
>     ↓
> 8. 启动时初始化 BeanFactory
>     ↓
> 9. 加载 BeanDefinition（@ComponentScan）
>     ↓
> 10. BeanFactoryPostProcessor 修改 BeanDefinition
>     ↓
> 11. 实例化 Bean（refresh() 里的 finishBeanFactoryInitialization()）
>     ↓
> 12. 生命周期回调（SmartInitializingSingleton）
>     ↓
> 13. 发送 ContextRefreshedEvent
> ```
>
> **SpringBoot 相比 Spring 的增强**：
> - 自动配置（@EnableAutoConfiguration）
> - 内嵌 Tomcat/Jetty
> - starter 依赖自动引入
> - 零 XML 配置（application.yml 即可）

---

### 2.6 Spring 循环依赖怎么解决

**前置概念：什么是循环依赖**

```
A 依赖 B，B 依赖 A
构造器注入 + setter 注入都可能
```

**三级缓存机制**：

| 缓存 | 作用 | 时机 |
|------|------|------|
| **singletonObjects**（一级） | 完整 Bean 实例 | 最终放到这里 |
| **earlySingletonObjects**（二级） | 半成品 Bean（属性未注入） | Bean 创建中提前暴露 |
| **singletonFactories**（三级） | Bean 工厂对象 | 创建中，还没调用构造器 |

**解决流程图**：
```
A 创建中：
  1. A 发现依赖 B，先查一级缓存，没有
  2. 开始创建 A，把 A 的工厂放入三级缓存
  3. 去创建 B

B 创建中：
  4. B 发现依赖 A，查一级，没有
  5. 查二级，没有
  6. 查三级，找到 A 的工厂，取出 A（半成品）放入二级缓存
  7. B 拿到 A，完成创建，放入一级缓存
  8. B 返回，A 拿到 B，完成创建

A 创建完成放入一级缓存
```

**三级缓存 vs 二级缓存**：

| 方案 | 能不能解决 | 说明 |
|------|-----------|------|
| 一级缓存 | ❌ | 没法提前暴露半成品 |
| 二级缓存 | ⚠️ 可以 | 可以，但拿到的可能不是最终版本 |
| 三级缓存 | ✅ | 可以，支持代理（AOP） |

**为什么需要三级缓存**：
- 二级缓存拿到的是 raw 对象
- 三级缓存工厂方法可以返回**代理对象**
- 如果有 AOP 增强，最终拿到的是代理而不是原始对象

**面试回答**：

> Spring 用三级缓存解决循环依赖。
>
> 对象创建时，先把工厂（lambda）放到三级缓存，属性注入时发现依赖另一个对象，就从缓存里拿出半成品。如果这个对象还有代理需求，工厂会返回代理对象。
>
> **能解决**：setter 注入的循环依赖（单例）。
>
> **不能解决**：
> 1. 构造器注入（还没创建完就要注入）
> 2. prototype 作用域（每次都是新对象，不缓存）
> 3. @DependsOn 循环依赖

---

### 2.7 MyBatis 架构原理

**核心流程**：
```
SqlSessionFactoryBuilder
    ↓ 读取配置
SqlSessionFactory
    ↓ 打开 Session
SqlSession
    ↓ 执行 SQL
Executor
    ↓ 调用 StatementHandler
StatementHandler（参数处理 + SQL 执行 + 结果映射）
    ├── ParameterHandler（参数设置）
    ├── ResultSetHandler（结果映射）
    └── MappedStatement（SQL + 结果类型）
```

**四大对象（可被插件拦截）**：

| 对象 | 作用 | 插件拦截点 |
|------|------|-----------|
| Executor | 执行器，SQL执行入口 | query / update |
| StatementHandler | 处理 SQL 语句 | prepare / parameterize / query / update |
| ParameterHandler | 设置参数 | setParameters |
| ResultSetHandler | 处理结果集 | handleResultSets / handleOutputParameters |

**插件机制（InterceptorChain）**：
```java
// 每个对象创建时，都会调用插件链
item = (T) interceptorChain.pluginAll(item);
// pluginAll 逐个调用 Interceptor.plugin()

@Intercepts({
    @Signature(type = StatementHandler.class, method = "prepare", args = {Connection.class})
})
public class MyPlugin implements Interceptor {
    @Override
    public Object plugin(Object target) {
        return Plugin.wrap(target, this);  // 生成代理对象
    }
}
```

**MyBatis 一级缓存 vs 二级缓存**：

| 缓存 | 作用域 | 失效 |
|------|--------|------|
| **一级缓存** | SqlSession（同一个会话） | flush 后失效 |
| **二级缓存** | SqlSessionFactory（跨会话） | flush 后失效，commit 时写入 |

**MyBatis 执行流程**：
```
SqlSession.selectOne("mapper.findById", 1)
    ↓
Executor.query() → 一级缓存查一下
    ↓
MappedStatement 拿到 SQL
    ↓
StatementHandler.prepare() → 预编译 SQL
    ↓
ParameterHandler.setParameters() → 设置参数
    ↓
JDBC 执行
    ↓
ResultSetHandler.handleResults() → 结果映射
    ↓
返回结果
```

**面试回答**：

> MyBatis 核心是 Executor + StatementHandler。
>
> Executor 是执行器门面，内部调用 StatementHandler。StatementHandler 负责预编译 SQL、设置参数、执行、映射结果。
>
> 参数映射用 ParameterHandler，结果映射用 ResultSetHandler。这四个对象都可以被插件拦截，实现自定义逻辑。
>
> 我之前用过插件做分页和 SQL 日志打印。

---

## 三、Redis

### 3.1 Redis 数据类型

**回答**：

> 1. **String**：普通字符串，缓存、计数器、分布式锁（SETNX）
> 2. **Hash**：对象存储，field-value
> 3. **List**：列表，消息队列（BRPOP）
> 4. **Set**：去重，标签系统（交集 SINTER）
> 5. **ZSet**：排序，排行榜（ZADD）
> 6. **Bitmap**：位图，签到统计
> 7. **HyperLogLog**：基数统计，UV 统计
> 8. **GEO**：地理位置，附近的人

---

### 3.2 Redis 持久化机制

**回答**：

> **RDB（快照）**：
> - 定时 fork 子进程，将数据写入 RDB 文件
> - 适合大规模数据恢复，备份
> - 缺点：可能丢失最后一次快照后的数据
>
> **AOF（追加日志）**：
> - 每次写操作记录到 aof 文件
> - 三种策略：always/everysec/no
> - 数据完整性高，文件大，恢复慢
>
> **混合持久化**（Redis 4.0+）：
> - RDB + AOF，先用 RDB 快速恢复，再用 AOF 补齐

---

### 3.3 Redis 集群方案

**回答**：

> 1. **主从复制**：一主多从，从库读，主库写。缺点是主从切换需要人工介入。
>
> 2. **哨兵（Sentinel）**：监控主从，自动选主。缺点是只有一个主节点，写能力受限。
>
> 3. **Cluster（Redis 3.0+）**：去中心化，16384 slots，每个节点负责一部分 slots。投票机制判断节点故障。

---

### 3.4 Redis 分布式锁

**回答**：

> **SETNX 方案**：
> ```java
> String lockKey = "lock:order:" + orderId;
> String uuid = UUID.randomUUID().toString();
> // 加锁，30秒过期
> Boolean success = redis.setIfAbsent(lockKey, uuid, 30, TimeUnit.SECONDS);
> if (success) {
>     try {
>         // 业务逻辑
>     } finally {
>         // 释放锁（Lua 脚本保证原子性）
>         if ("uuid".equals(redis.get(lockKey))) {
>             redis.delete(lockKey);
>         }
>     }
> }
> ```
>
> **Redisson 方案**：
> ```java
> RLock lock = redisson.getLock("lockKey");
> lock.lock();
> try {
>     // 业务逻辑
> } finally {
>     lock.unlock();
> }
> ```
> Redisson 自动续期、看门狗机制。

---

### 3.5 Redis 缓存问题

**缓存穿透**：查询不存在的数据，每次都打数据库
> 解决：布隆过滤器、缓存空值

**缓存击穿**：热点 key 过期，瞬间大量请求打数据库
> 解决：互斥锁、永不过期+异步更新

**缓存雪崩**：大量 key 同时过期
> 解决：过期时间加随机值、热点数据不过期

---

## 四、MySQL

### 4.1 索引类型

**回答**：

> 1. **主键索引**：InnoDB 表必须有主键，数据和索引在一起（聚簇索引）
> 2. **唯一索引**：唯一约束，叶子节点存索引值
> 3. **普通索引**：普通字段，叶子节点存主键值
> 4. **联合索引**：多字段组合，遵循最左前缀原则
> 5. **全文索引**：MATCH AGAINST，文本搜索

---

### 4.1.1 什么是二级索引

聚簇索引（主键索引）以外的所有索引，都叫二级索引。

**和主键索引的区别**：
```
主键索引（聚簇索引）：
  B+Tree 叶子节点 = 整行数据

二级索引（普通索引）：
  B+Tree 叶子节点 = 主键 ID
  拿到主键后，再去主键索引找整行 → 回表
```

```sql
-- 主键：直接拿到整行数据
select * from orders where id = 100;

-- 二级索引（user_id 建的普通索引）：
-- 先查 user_id 索引 → 拿到主键 id → 再查主键索引 → 拿到整行
select * from orders where user_id = 200;
```

**回表**：二级索引查到主键后，再去主键索引查整行数据，多一次磁盘 IO。

**覆盖索引**：查的字段全在索引里，不用回表。

```sql
-- 有联合索引 idx_user_status(user_id, status)

-- 回表：select * 要走主键索引再查一次
select * from orders where user_id = 200;

-- 覆盖索引：要的字段都在索引里，不回表
select id, user_id, status from orders where user_id = 200;
```

---

### 4.2 InnoDB 和 MyISAM 区别

| 对比 | InnoDB | MyISAM |
|------|--------|--------|
| 事务 | 支持 | 不支持 |
| 锁 | 行锁 | 表锁 |
| 外键 | 支持 | 不支持 |
| 全文索引 | 5.6+支持 | 支持 |
| 存储结构 | 聚簇索引 | 非聚簇索引 |
| MVCC | 支持 | 不支持 |
| 崩溃恢复 | 自动恢复 | 慢 |

---

### 4.3 MVCC 原理

**回答**：

> **核心原理**：为每条数据保存多个历史版本，让**读操作不阻塞写，写操作不阻塞读**。
>
> **关键实现（以InnoDB为例）：**
>
> 1. **版本链**：每行数据隐藏两个字段：
>
> - - `DB_TRX_ID`：最后修改它的事务ID。
>   - `DB_ROLL_PTR`：指向上一个版本的指针，形成版本链。
>
> 1. **ReadView**：事务在**快照读**（普通SELECT）时生成一个“读视图”，包含：
>
> - - `m_ids`：当前活跃（未提交）的事务ID列表。
>   - `min_trx_id`：最小活跃事务ID。
>   - `max_trx_id`：系统将分配的下一个事务ID。
>   - `creator_trx_id`：创建此ReadView的事务自身ID。
>
> 1. **可见性判断**：遍历版本链，用ReadView规则判断哪个版本对当前事务可见：
>
> - - 如果版本的事务ID `< min_trx_id`，说明在ReadView创建前已提交 → **可见**。
>   - 如果版本的事务ID `>= max_trx_id`，说明在ReadView创建后才开启 → **不可见**。
>   - 如果版本的事务ID在 `m_ids`中，说明事务还未提交 → **不可见**。
>   - 如果版本的事务ID是创建者自身的事务 → **可见**。
>
> **两种读**：
>
> - **快照读**：普通SELECT，读取历史版本，实现可重复读。
> - **当前读**：`SELECT ... FOR UPDATE`、`INSERT`、`UPDATE`、`DELETE`，读取最新已提交数据并加锁。



### 4.5 分库分表

**回答**：

> **垂直拆分**：按业务拆表，把字段多的表拆成多个
>
> **水平拆分**：按某个字段（如 user_id）哈希分片
>
> **ShardingSphere**：
> - 配置逻辑表到实际表映射
> - 按 order_sn 哈希分片到 16 张表（_0 到 _f）
> - 分片算法：`hash(order_sn) % 16`
>
> **问题**：
> - 跨表查询麻烦
> - 分页困难
> - 分布式事务问题

---

### 4.6 MySQL 慢查询优化

**回答**：

> 慢查询优化一般分四步：**慢 SQL 定位 → explain 分析 → 索引优化 → SQL 改写**。

**1. 定位慢 SQL**：
```sql
-- 开启慢查询日志
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 1  -- 超过 1 秒记录

-- 查看慢查询
show variables like 'slow_query%';
show variables like 'long_query_time';

-- 分析慢查询日志
mysqldumpslow -s t -t 10 /var/log/mysql/slow.log
```

**2. explain 分析**：
```sql
explain select * from orders where user_id = 123 and status = 'paid';
```

关键字段：
| 字段 | 怎么看 |
|------|--------|
| **type** | const > eq_ref > ref > range > index > ALL（尽量不是 ALL） |
| **key** | 显示用了哪个索引 |
| **rows** | 扫描行数，越少越好 |
| **Extra** | Using filesort / Using temporary 是bad sign |

**3. 索引失效场景**：
| 场景 | 原因 |
|------|------|
| `%开头模糊匹配` | B+Tree 按前缀匹配，开头模糊无法定位 |
| `函数包裹列` | 索引存原始值，函数后无法匹配 |
| `隐式类型转换` | 字符串列用数字查，MySQL 自动转函数 |
| `最左前缀中断` | 联合索引 (a,b,c)，跳过 a 查 b 不走索引 |
| 联合索引范围右边失效 | (a,b,c)，`b>5` 后 c 不走索引 |
|  |  |

**举例说明**：

**函数包裹列**：
```sql
-- create_time 有索引，但 year() 函数把索引值转成了年份，索引失效
-- 低效
select * from orders where year(create_time) = 2025;

-- 改成范围查询，走索引
select * from orders where create_time >= '2025-01-01' and create_time < '2026-01-01';
```

**隐式类型转换**：
```sql
-- phone 是 varchar 类型，有索引
-- 用数字查，左边varchar右边int，MySQL 把 phone 转了int，索引失效
-- 低效
select * from users where phone = 13800138000;

-- 加上引号，varchar 直接匹配索引
select * from users where phone = '13800138000';
```

**4. 常见优化手段**：
```sql
-- 1. 加索引
alter table orders add index idx_user_status(user_id, status);

-- 2. 避免 SELECT *
select id, status from orders where user_id = 123;

-- 3. 优化 limit
-- 低效：select * from orders limit 1000000, 10
-- 高效：select * from orders where id > 1000000 limit 10

-- 4. 分页优化（深度分页）
-- 低效：select * from orders order by id limit 1000000, 10
-- 高效：select * from orders where id > #{last_id} limit 10
```

**5. 面试回答**：

> 我一般先用 `explain` 看执行计划，重点关注 type、key、rows、Extra。
>
> type 如果是 ALL 说明全表扫描，需要加索引。Extra 出现 Using filesort 说明排序没走索引。
>
> 索引失效常见场景：函数包裹、最左前缀中断、模糊匹配 `%` 开头。我一般用覆盖索引优化，减少回表。

---

## 五、消息队列

### 5.1 RabbitMQ 和 RocketMQ 区别

| 对比 | RabbitMQ | RocketMQ |
|------|----------|----------|
| 开发语言 | Erlang | Java |
| 单机吞吐量 | 万级 | 十万级 |
| 延迟 | 微秒级 | 毫秒级 |
| 事务消息 | 无（插件实现） | 原生支持 |
| 延迟消息 | 支持（插件） | 原生支持（定时） |
| 死信队列 | 支持 | 支持 |
| 消息堆积 | 支持 | 支持 |

---

### 5.2 如何保证消息不丢失

**回答**：

> **三个环节**：
>
> 1. **生产者发送时**：
> - 确认机制（Publisher Confirm）
> - 失败重试
>
> 2. **MQ 存储时**：
> - 持久化到磁盘（durable）
> - 同步刷盘策略
>
> 3. **消费者消费时**：
> - 手动 ACK（确认消费完成才删除）
> - 幂等控制（重复消费不影响）

---

### 5.3 顺序消息

**回答**：

> **RocketMQ 顺序消息**：
> - 生产者：同一个订单的消息发到同一个 MessageQueue
> - 消费者：使用 MessageListenerOrderly，锁定队列顺序消费
>
> **实现方式**：
> ```java
> // 生产者：按订单 ID 哈希选择队列
> MessageQueueSelector selector = new HashOrderSelector();
> producer.send(msg, selector, orderId);
>
> // 消费者：实现 OrderListener
> public class OrderListener implements MessageListenerOrderly {
>     @Override
>     public ConsumeOrderlyStatus consumeMessage(List<MessageExt> msgs, ConsumeOrderlyContext context) {
>         // 顺序处理
>     }
> }
> ```

---

### 5.4 消息幂等

**回答**：

> **为什么需要幂等**：MQ 可能重复投递，消费者必须能处理重复消息。
>
> **解决方案**：
> 1. **数据库唯一索引**：消费前先 insert，重复插入报错
> 2. **Redis 去重**：消费前查 Redis 是否处理过
> 3. **幂等 token**：前端提交时带 token，后端校验后删除
> 4. **状态机**：订单状态只能顺序流转，取消→已取消，不能再变

---

### 5.5 延迟消息（定时消息）

**是什么**：消息发送后，不立即投递，而是等指定时间后才投递给消费者。

**应用场景**：
- 订单超时关闭（下单后15分钟未支付，关闭订单）
- 任务重试（失败后延迟重试）
- 定时调度（每天定时发送报表）

**设置方式**：

**RocketMQ 5.0**（设置目标时间戳）：
```java
// 设置延迟时间 = 当前时间 + 延迟时长，转成时间戳
Long deliverTimeStamp = System.currentTimeMillis() + 10L * 60 * 1000; // 10分钟后

Message message = messageBuilder.setTopic("topic")
    .setDeliveryTimestamp(deliverTimeStamp)  // 设置目标时间戳
    .setBody("messageBody".getBytes())
    .build();
```

**RocketMQ 4.x**（设置延迟时间毫秒值）：
```java
BaseSendExtendDTO.builder()
    .delayTime(10 * 60 * 1000L)  // 延时10分钟（毫秒）
    .build();
```

**项目中的应用**：
```java
// 订单延时关闭，10分钟后自动取消未支付订单
.orderDelayCloseProducer.sendMessage(OrderDelayCloseMqDTO.builder()
    .orderSn(orderSn)
    .build());
```

**面试回答**：

> 延迟消息是指定时消息，消息发送后不立即投递，而是等指定时间后才投递给消费者。
>
> **设置方式**：`当前时间 + 延迟时长`，转成时间戳（毫秒级Unix时间戳）。
>
> **应用场景**：订单超时关闭。下单后用户未支付，10分钟后自动关闭订单。避免用户占用库存但不付款。
>
> **我们的项目里**：用延迟消息实现订单15分钟未支付自动关闭，释放库存。

---

## 六、微服务

### 6.1 Spring Cloud 核心组件

| 组件 | 作用 |
|------|------|
| Eureka/Nacos | 注册中心，服务注册与发现 |
| Feign/OpenFeign | 声明式 HTTP 客户端 |
| Ribbon | 负载均衡 |
| Hystrix/Sentinel | 熔断降级 |
| Gateway | API 网关 |
| Config/Nacos Config | 配置中心 |
| Sleuth/Zipkin | 链路追踪 |

---

### 6.2 CAP 定理

**回答**：

> 分布式系统最多只能同时满足两个：
>
> - **C（Consistency）**：一致性，所有节点数据一致
> - **A（Availability）**：可用性，每次请求都有响应
> - **P（Partition tolerance）**：分区容错，分区之间通信可能失败
>
> **取舍**：
> - CP：放弃可用性，追求一致性（Zookeeper、HBase）
> - AP：放弃强一致，追求可用性（Eureka、Cassandra）
>
> 实际系统要根据业务选择，不是非此即彼。

---

### 6.3 Nacos 和 Eureka 区别

| 对比 | Nacos | Eureka |
|------|-------|--------|
| 一致性 | 支持 CP 和 AP | 只能 AP |
| 心跳检测 | TCP / HTTP | HTTP |
| 服务权重 | 支持 | 不支持 |
| 标签/元数据 | 支持 | 支持 |
| 配置中心 | 支持 | 不支持 |
| 界面 | 有 | 无（需第三方） |

---

### 6.4 分布式事务解决方案

**回答**：

> 1. **2PC（两阶段提交）**：协调者发起 Prepare，所有参与者执行但不提交，然后协调者发起 Commit。
>    - 缺点：同步阻塞、单点故障、数据不一致
>
> 2. **TCC（Try-Confirm-Cancel）**：业务层面实现三阶段
>    - Try：预留资源
>    - Confirm：确认使用
>    - Cancel：取消回滚
>    - 缺点：代码侵入性高
>
> 3. **本地消息表 + 定时任务**：订单库创建时同时插消息表，发 MQ 后更新状态，定时任务扫描未确认的消息重试
>
> 4. **Seata**：AT 模式，自动补偿回滚，对业务代码无侵入

---

## 七、Docker / K8s

### 7.1 Docker 常用命令

**前置概念**：

| 概念 | 类比 | 说明 |
|------|------|------|
| **镜像（Image）** | 安装包 / 类 | 只读模板，包含运行环境 + 代码，不动 |
| **容器（Container）** | 运行中的程序 / 对象 | 镜像启动后的实例，可读写 |

```
docker pull nginx    →   下载 nginx 安装包（镜像）
docker run nginx     →   双击安装包，启动程序（容器）
```

**镜像操作**：
```bash
docker pull nginx              # 下载 nginx 镜像（从 Docker Hub 拉）
docker images                  # 查看本地有哪些镜像
docker rmi nginx               # 删除 nginx 镜像（rmi = remove image）
docker build -t myapp .        # 用当前目录的 Dockerfile 构建镜像，取名 myapp
```

**容器操作**：
```bash
# 启动容器
docker run -d -p 8080:80 --name web nginx
#  -d       后台运行
#  -p 8080:80  宿主机8080端口 → 容器内部80端口
#  --name web  容器取名"web"
#  nginx       用的镜像

docker ps                       # 查看正在运行的容器（加 -a 看全部包括停止的）
docker stop web                 # 停止 web 容器（容器还在，可以重新 start）
docker rm web                   # 删除 web 容器（rm = remove，删了就没了）
```

**调试**：
```bash
docker logs -f web              # 实时看 web 容器的日志（-f = follow）
docker exec -it web /bin/bash   # 进到 web 容器里面，像 SSH 一样（-it = 交互终端）
```

---

### 7.2 Dockerfile 指令

```dockerfile
FROM openjdk:8        # 基础镜像
COPY target/*.jar /app.jar   # 复制文件
WORKDIR /app            # 工作目录
EXPOSE 8080             # 暴露端口
ENV JAVA_OPTS="-Xms256m"  # 环境变量
RUN mvn package         # 构建
ENTRYPOINT ["java", "-jar", "/app.jar"]  # 启动命令
```

---

### 7.3 K8s 核心概念

| 概念 | 作用 |
|------|------|
| Pod | 最小调度单位，包含一个或多个容器 |
| Deployment | 管理 Pod 副本数、滚动更新 |
| Service | 负载均衡，服务发现 |
| Ingress | HTTP/HTTPS 路由 |
| ConfigMap/Secret | 配置管理 |
| PersistentVolume | 持久化存储 |

---

## 八、设计模式

### 8.1 单例模式

**是什么**：确保一个类只有一个实例，并提供全局访问点。

**应用场景**：
- 数据库连接池
- 配置类（ApplicationConfig）
- 线程池
- Redis 客户端（Redisson）

**回答**：

> 单例模式保证一个类只有一个实例。
>
> **写法**：双重检锁 + volatile
> - 第一层 if 判断避免不必要的同步
> - synchronized 加锁保证线程安全
> - volatile 防止指令重排序（new Singleton() 不是原子操作）
>
> **为什么双重检锁**：
> - 外层 if：实例已经创建了，直接返回，不用抢锁
> - 内层 if：抢到锁的线程再次确认，避免重复创建

**代码**：
```java
public class Singleton {
    private static volatile Singleton instance;

    private Singleton() {}

    public static Singleton getInstance() {
        if (instance == null) {           // 第一层：避免不必要的同步
            synchronized (Singleton.class) {
                if (instance == null) {   // 第二层：确认实例未创建
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

---

### 8.2 工厂模式

**是什么**：定义一个创建对象的接口，让子类决定实例化哪个类。

**三种形态**：
1. **简单工厂**：一个工厂类根据参数创建不同产品（违反开闭原则）
2. **工厂方法**：抽象工厂，每个产品对应一个工厂子类
3. **抽象工厂**：一个工厂生成一系列相关产品

**应用场景**：
- Calendar.getInstance() 获取不同地区日历
- MyBatis SqlSessionFactory 创建 SqlSession
- Spring BeanFactory 根据 id 创建 bean

**回答**：

> 工厂模式把对象创建从调用方分离出来。
>
> **简单工厂**：一个工厂类根据类型参数创建产品。优点是调用方不用 new，缺点是加新产品要改工厂类。
>
> **工厂方法**：抽象工厂接口，每个产品有自己的工厂。优点是符合开闭原则，缺点是类数量增多。
>
> **我们的项目里**：Dubbo 封装了工厂模式，用户调用接口不需要关心具体实现类。

---

### 8.3 策略模式

**是什么**：定义一系列算法，把它们一个个封装起来，让它们可以互相替换。

**应用场景**：
- 支付方式选择（支付宝/微信/银行卡）
- 排序算法选择（快排/归并/冒泡）
- 促销活动计算（满减/折扣/立减）

**回答**：

> 策略模式把if-else分支的每个分支封装成独立的策略类，可以动态替换。
>
> **结构**：
> - Strategy：策略接口
> - ConcreteStrategy：具体策略实现
> - Context：持有策略引用，调用策略
>
> **优点**：符合开闭原则，新加策略不用改原有代码；消除if-else；策略可以动态切换。
>
> **我们的项目里**：用策略模式封装不同的缓存处理逻辑，根据场景选择不同的缓存策略。

**代码**：
```java
// 策略接口
public interface Strategy {
    int doOperation(int a, int b);
}

// 具体策略
public class AddStrategy implements Strategy {
    @Override
    public int doOperation(int a, int b) { return a + b; }
}

public class SubtractStrategy implements Strategy {
    @Override
    public int doOperation(int a, int b) { return a - b; }
}

// Context 使用策略
public class Context {
    private Strategy strategy;
    public Context(Strategy strategy) { this.strategy = strategy; }
    public int execute(int a, int b) { return strategy.doOperation(a, b); }
}
```

---

### 8.4 责任链模式

**是什么**：将请求沿着处理者链传递，每个处理者处理自己的逻辑，要么处理，要么传给下一个。

**应用场景**：
- Servlet Filter 处理请求过滤、登录校验、权限校验
- Spring Security 过滤器链
- 项目里的 Canal Binlog 处理器链

**回答**：

> 责任链模式把请求在处理者链上传递，每个处理者只关心自己的逻辑。
>
> **优点**：请求发送者和接收者解耦；可以动态添加/删除处理者；符合单一职责原则。
>
> **我们的项目里**：用责任链模式封装不同的缓存处理逻辑，比如先处理布隆过滤器、再处理Redis缓存、再处理数据库查询。

**代码**：
```java
// 抽象处理者
public abstract class Handler {
    protected Handler next;  // 下一个处理者

    public void setNext(Handler next) { this.next = next; }

    public final void handle(Request request) {
        if (doHandle(request)) {
            return;  // 处理了，结束
        }
        if (next != null) {
            next.handle(request);  // 传给下一个
        }
    }

    protected abstract boolean doHandle(Request request);
}

// 具体处理者
public class AuthHandler extends Handler {
    @Override
    protected boolean doHandle(Request request) {
        if (request.isLogin()) {
            return false;  // 没登录，传给下一个
        }
        return true;  // 已登录，处理结束
    }
}
```

---

### 8.5 模板方法模式

**是什么**：把算法骨架定义在抽象类中，具体步骤延迟到子类实现。

**应用场景**：
- JDK InputStream.read() 抽象方法，子类实现具体读取
- Spring AbstractApplicationContext.refresh() 定义生命周期
- 项目里 MQ 发送消息的封装

**回答**：

> 模板方法把算法的不变部分封装到父类，可变部分延迟到子类。
>
> **核心**：用 final 定义算法骨架方法，用 abstract 定义可变步骤。
>
> **我们的项目里**：AbstractCommonSendProduceTemplate 定义了消息发送的完整流程，子类只需要实现 buildMessage() 和 buildBaseSendExtendParam() 两个方法。

---

### 8.6 代理模式

**是什么**：为对象提供一个代理，控制对原对象的访问。调用方不直接访问目标对象，而是通过代理对象间接访问。

**应用场景**：
- Spring AOP（切面编程：事务、缓存、幂等）
- MyBatis 懒加载
- RPC 框架（远程代理）

---

#### JDK 动态代理

**原理**：被代理类**必须实现接口**，运行时生成代理类和目标类实现相同接口。

**核心类**：`java.lang.reflect.Proxy`、`InvocationHandler`

**代码**：
```java
// 1. 定义接口
public interface UserService {
    void saveUser(String name);
}

// 2. 目标类（实现接口）
public class UserServiceImpl implements UserService {
    @Override
    public void saveUser(String name) {
        System.out.println("保存用户：" + name);
    }
}

// 3. InvocationHandler（定义代理逻辑）
public class MyInvocationHandler implements InvocationHandler {
    private Object target;  // 目标对象

    public MyInvocationHandler(Object target) {
        this.target = target;
    }

    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        System.out.println("前置逻辑...");
        Object result = method.invoke(target, args);  // 调用目标方法
        System.out.println("后置逻辑...");
        return result;
    }
}

// 4. 生成代理对象
UserService proxy = (UserService) Proxy.newProxyInstance(
    UserService.class.getClassLoader(),     // 类加载器
    new Class[]{UserService.class},        // 接口数组
    new MyInvocationHandler(new UserServiceImpl())  // InvocationHandler
);

// 5. 调用代理方法
proxy.saveUser("张三");
```

**执行流程**：
```
调用 proxy.saveUser("张三")
    ↓
Proxy.invoke() 被调用
    ↓
执行前置逻辑
    ↓
method.invoke(target, args)  调用目标方法
    ↓
执行后置逻辑
```

---

#### CGLIB 代理

**原理**：被代理类**不需要实现接口**，运行时生成目标类的**子类**，通过继承生成代理对象。

**核心类**：`net.sf.cglib.proxy.Enhancer`、`MethodInterceptor`

**代码**：
```java
// 1. 目标类（不需要实现接口）
public class UserService {
    public void saveUser(String name) {
        System.out.println("保存用户：" + name);
    }
}

// 2. MethodInterceptor（定义代理逻辑）
public class MyMethodInterceptor implements MethodInterceptor {
    @Override
    public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable {
        System.out.println("前置逻辑...");
        Object result = proxy.invokeSuper(obj, args);  // 调用父类方法（目标方法）
        System.out.println("后置逻辑...");
        return result;
    }
}

// 3. 生成代理对象
Enhancer enhancer = new Enhancer();
enhancer.setSuperclass(UserService.class);     // 设置父类
enhancer.setCallback(new MyMethodInterceptor());  // 设置回调
UserService proxy = (UserService) enhancer.create();

// 4. 调用代理方法
proxy.saveUser("张三");
```

---

#### JDK 代理 vs CGLIB 代理

| 对比 | JDK 动态代理 | CGLIB 代理 |
|------|-------------|------------|
| **实现方式** | 实现接口，生成代理类 | 继承父类，生成子类 |
| **被代理类要求** | 必须实现接口 | 不需要，可代理任意类 |
| **生成方式** | Proxy.newProxyInstance() | Enhancer.create() |
| **执行速度** | 反射调用，略慢 | 直接调用，快一些 |
| **Spring 默认** | 否 | 是（Spring 5.x 前用 JDK，之后默认 CGLIB） |
| **final 类/方法** | 可代理 final 类 | 不能代理 final 类/final 方法 |

**面试回答**：

> 代理模式不直接访问对象，而是通过代理对象间接访问。
>
> **JDK 动态代理**：被代理类必须实现接口，运行时生成实现相同接口的代理类。Proxy.newProxyInstance() 创建代理对象。
>
> **CGLIB 代理**：被代理类不需要接口，通过继承生成子类。Enhancer.create() 创建代理对象。
>
> **Spring 选择**：
> - 如果目标类有接口，Spring 会用 JDK 动态代理
> - 如果目标类没有接口，Spring 用 CGLIB 生成子类代理
> - Spring Boot 2.x 默认使用 CGLIB
>
> **我们的项目里**：Spring AOP 用代理模式实现事务、缓存、幂等等功能。方法增强时，如果类实现了接口就走 JDK 代理，没实现接口就走 CGLIB。

---

## 九、架构设计

### 9.1 分布式 ID 方案

| 方案 | 优点 | 缺点 |
|------|------|------|
| UUID | 简单，不依赖外部 | 无序、字符串、无法用于分库分表 |
| 数据库自增 | 有序、数字 | 依赖单库，无法水平扩展 |
| 雪花算法 | 有序、数字、不依赖外部 | 依赖时钟 |
| 滴滴 TinyID | 搭建发号服务，支持多库 | 依赖 DB |

---

### 9.2 如何设计一个秒杀系统

**回答**：

> 1. **前端限流**：验证码、按钮防重复点击
> 2. **网关限流**：滑动窗口 / 令牌桶，限制 QPS
> 3. **Redis 预扣减库存**：Lua 脚本原子操作
> 4. **MQ 异步下单**：库存扣成功后发 MQ，异步生成订单
> 5. **数据库乐观锁**：update stock set stock = stock - 1 where id = ? and stock > 0
> 6. **熔断降级**：库存不足时快速返回

---

## 十、计算机基础

### 10.1 TCP 三次握手 / 四次挥手

**三次握手**：
```
客户端：SYN=1, seq=x                → 服务端
客户端：SYN=1, ACK=1, seq=y, ack=x+1 ← 服务端
客户端：ACK=1, seq=x+1, ack=y+1      → 服务端
```

**四次挥手**：
```
主动方：FIN=1, seq=u        → 被动方
被动方：ACK=1, seq=v, ack=u+1 ← 被动方
被动方：FIN=1, seq=w        ← 被动方（数据发送完毕）
主动方：ACK=1, seq=u+1, ack=w+1 → 被动方
```

---

### 10.2 HTTP 和 HTTPS 区别

| 对比 | HTTP | HTTPS |
|------|------|-------|
| 端口 | 80 | 443 |
| 传输 | 明文 | 加密（SSL/TLS） |
| 证书 | 不需要 | 需要 CA 证书 |
| 性能 | 快 | 慢（握手耗时） |
| OSI层 | 应用层 | 传输层 |

---

### 10.3 Cookie 和 Session 区别

| 对比 | Cookie | Session |
|------|--------|---------|
| 存储位置 | 浏览器 | 服务器 |
| 容量 | 4KB | 无限制 |
| 安全性 | 较低（明文） | 较高 |
| 跨域 | 支持 | 不支持 |
| 生命周期 | 可设置 | 随浏览器关闭或超时 |

---

### 10.4 Linux 常用命令

**文件操作**：
```bash
ls                    # 列出当前目录的文件
ls -la                # 详细信息（-l）+ 隐藏文件（-a）
cd /opt/app           # 进到 /opt/app 目录
pwd                   # 显示当前目录路径
mkdir logs            # 创建 logs 目录
rm file.txt           # 删除文件
rm -rf logs/          # 强制删除目录（递归删全部子文件）
cp a.txt b.txt        # 复制文件
mv old.txt new.txt    # 重命名（或在同目录下就是改名）
```

**查看文件**：
```bash
cat app.log             # 一次性显示全部内容
head -20 app.log        # 看前 20 行
tail -f app.log         # 实时看末尾追加的内容（-f = follow，Ctrl+C 退出）
less app.log            # 翻页看（q 退出）
```

**搜索**：
```bash
grep "error" app.log           # 搜 error 关键字
grep "error" app.log -n        # 带行号
grep -v "debug" app.log        # 排除 debug 行
grep "error" app.log | wc -l   # 统计 error 出现多少次
```

**进程和资源**：
```bash
ps aux | grep java     # 查 java 进程
top                    # 实时看 CPU/内存占用（q 退出）：
```

**管道和重定向**：
```bash
ls | grep ".txt"                  # 把 ls 的结果传给 grep 过滤
cat app.log | grep "error" > err.log    # > 覆盖写入文件
cat app.log | grep "error" >> err.log   # >> 追加到文件末尾
./app > /dev/null 2>&1 &          # 后台静默启动，不输出日志
```

---

## 十一、FastAPI

### 11.1 FastAPI 核心特性

**回答**：

> FastAPI 是一个现代、高性能的 Python Web 框架，基于 Starlette（异步）和 Pydantic（数据验证）。
>
> **核心特性**：
> 1. **异步支持**：原生 async/await，性能接近 NodeJS 和 Go
> 2. **自动文档**：自动生成 OpenAPI/Swagger UI 和 ReDoc
> 3. **类型提示**：基于 Python 类型注解，自动数据验证
> 4. **依赖注入**：强大的 DI 系统，管理数据库连接、认证等
> 5. **Pydantic 集成**：自动请求/响应验证，JSON序列化
> 6. **标准兼容**：兼容 WSGI（同步）和 ASGI（异步）

---

### 11.2 FastAPI 与 Flask/Django 区别

| 对比 | FastAPI | Flask | Django |
|------|---------|-------|--------|
| 异步 | 原生支持 | 需扩展 | 需扩展 |
| 性能 | 极高（接近 Go） | 中等 | 中等 |
| 自动化文档 | 原生生成 | 需扩展 | 需扩展 |
| 数据验证 | Pydantic | WTForms | Forms |
| 适用场景 | API、微服务 | 小型应用 | 大型全栈 |

**面试回答**：

> FastAPI 和 Flask 都是轻量级框架，但 FastAPI 原生支持异步，性能更高。
>
> Django 是全栈框架，自带 ORM、Admin、模板等，适合大型项目。
>
> **为什么选 FastAPI**：
> - 性能高，异步处理并发请求
> - 自动生成 API 文档，减少沟通成本
> - 类型提示支持 IDE 自动补全
> - 依赖注入系统方便测试

---

### 11.3 Pydantic 模型

**回答**：

> Pydantic 是 Python 数据验证库，FastAPI 用它做请求/响应验证。
>
> **两种模型**：
> - `BaseModel`：基础模型，定义数据结构
> - `Field`：字段验证，如 `Field(..., min_length=1)`
>
> **示例**：
> ```python
> from pydantic import BaseModel, Field
>
> class UserCreate(BaseModel):
>     username: str = Field(..., min_length=3, max_length=50)
>     email: str = Field(..., regex=r'^[\w\.-]+@[\w\.-]+\.\w+$')
>     age: int | None = Field(default=18, ge=0, le=150)
>
> class UserResponse(BaseModel):
>     id: int
>     username: str
>     email: str
>     class Config:
>         from_attributes = True  # ORM 模式
> ```
>
> **请求验证**：FastAPI 自动把 JSON 转成 Pydantic 模型，验证失败返回 422 错误。
>
> **响应验证**：返回 Pydantic 模型时，自动序列化成 JSON。

---

### 11.4 依赖注入系统

**回答**：

> FastAPI 的依赖注入系统管理数据库连接、认证、缓存等共享资源。
>
> **原理**：把依赖作为函数参数，FastAPI 自动解析并注入。
>
> **示例**：
> ```python
> from fastapi import Depends, HTTPException
>
> # 简单依赖
> def get_db():
>     db = SessionLocal()
>     try:
>         yield db
>     finally:
>         db.close()
>
> # 带验证的依赖
> async def verify_token(token: str = Header(...)):
>     if not token.startswith("Bearer "):
>         raise HTTPException(status_code=401, detail="Invalid token")
>     return token
>
> # 路由中使用
> @app.get("/users")
> def get_users(db: Session = Depends(get_db), token: str = Depends(verify_token)):
>     return db.query(User).all()
> ```
>
> **常见依赖**：
> - `Depends(get_db)`：数据库连接
> - `Header(...)`：请求头参数
> - `Query(...)`：查询参数
> - `Body(...)`：请求体
>
> **好处**：方便单元测试（可以 mock 依赖）、代码复用、解耦。

---

### 11.5 异步编程

**回答**：

> FastAPI 支持同步和异步两种处理方式。
>
> **同步视图**：
> ```python
> @app.get("/sync")
> def sync_view():
>     return {"message": "sync"}
> ```
>
> **异步视图**：
> ```python
> @app.get("/async")
> async def async_view():
>     result = await some_async_operation()
>     return result
> ```
>
> **选择**：
> - 有 `await` 操作（数据库查询、HTTP请求、文件IO）用 `async def`
> - 只有 CPU 计算用 `def`
>
> **ORM 异步**：
> ```python
> from sqlalchemy.ext.asyncio import AsyncSession
>
> @app.get("/users/{user_id}")
> async def get_user(user_id: int, db: AsyncSession = Depends(get_db)):
>     result = await db.execute(select(User).where(User.id == user_id))
>     user = result.scalar_one_or_none()
>     return user
> ```

---

### 11.6 中间件

**回答**：

> 中间件是在请求到达路由之前或响应返回之前执行的函数。
>
> **内置中间件**：
> - `CORSMiddleware`：跨域资源共享
> - `SessionMiddleware`：会话管理
> - `GZipMiddleware`：压缩响应
>
> **自定义中间件**：
> ```python
> from starlette.middleware.base import BaseHTTPMiddleware
>
> class MyMiddleware(BaseHTTPMiddleware):
>     async def dispatch(self, request, call_next):
>         # 请求处理前
>         print(f"请求路径: {request.url.path}")
>
>         response = await call_next(request)
>
>         # 响应处理后
>         response.headers["X-Custom-Header"] = "value"
>         return response
>
> app.add_middleware(MyMiddleware)
> ```
>
> **执行顺序**：中间件按添加顺序执行，最后添加的最先执行（洋葱模型）。

---

### 11.7 安全性（JWT 认证）

**回答**：

> FastAPI 内置 OAuth2 密码流程和 JWT 支持。
>
> **JWT 认证示例**：
> ```python
> from fastapi import FastAPI, Depends, HTTPException, status
> from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm
> from jose import JWTError, jwt
> from datetime import datetime, timedelta
>
> SECRET_KEY = "secret"
> ALGORITHM = "HS256"
> ACCESS_TOKEN_EXPIRE_MINUTES = 30
>
> oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")
>
> def create_access_token(data: dict):
>     to_encode = data.copy()
>     expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
>     to_encode.update({"exp": expire})
>     return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
>
> async def get_current_user(token: str = Depends(oauth2_scheme)):
>     try:
>         payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
>         username: str = payload.get("sub")
>         if username is None:
>             raise HTTPException(status_code=401, detail="Invalid token")
>     except JWTError:
>         raise HTTPException(status_code=401, detail="Invalid token")
>
> @app.post("/token")
> async def login(form_data: OAuth2PasswordRequestForm = Depends()):
>     # 验证用户密码...
>     access_token = create_access_token(data={"sub": form_data.username})
>     return {"access_token": access_token, "token_type": "bearer"}
>
> @app.get("/users/me")
> async def read_users_me(current_user: str = Depends(get_current_user)):
>     return {"username": current_user}
> ```

---

### 11.8 数据库集成（SQLAlchemy）

**回答**：

> FastAPI 可以集成 SQLAlchemy 进行数据库操作。
>
> **同步方式**：
> ```python
> from sqlalchemy import create_engine, Column, Integer, String
> from sqlalchemy.ext.declarative import declarative_base
> from sqlalchemy.orm import sessionmaker
>
> SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
>
> engine = create_engine(SQLALCHEMY_DATABASE_URL)
> SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
> Base = declarative_base()
>
> class User(Base):
>     __tablename__ = "users"
>     id = Column(Integer, primary_key=True, index=True)
>     name = Column(String)
>
> def get_db():
>     db = SessionLocal()
>     try:
>         yield db
>     finally:
>         db.close()
> ```
>
> **异步方式**（推荐）：
> ```python
> from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
>
> engine = create_async_engine("sqlite+aiosqlite:///./test.db")
>
> async def get_db():
>     async with AsyncSession(engine) as session:
>         yield session
> ```

---

### 11.9 异常处理

**回答**：

> FastAPI 提供统一的异常处理机制。
>
> **HTTPException**：
> ```python
> from fastapi import HTTPException
>
> @app.get("/items/{item_id}")
> async def get_item(item_id: int):
>     if item_id not in items:
>         raise HTTPException(status_code=404, detail="Item not found")
>     return items[item_id]
> ```
>
> **自定义异常处理器**：
> ```python
> from fastapi import Request, status
> from fastapi.responses import JSONResponse
>
> class UnicornException(Exception):
>     def __init__(self, name: str):
>         self.name = name
>
> @app.exception_handler(UnicornException)
> async def unicorn_exception_handler(request: Request, exc: UnicornException):
>     return JSONResponse(
>         status_code=status.HTTP_418_IM_A_TEAPOT,
>         content={"message": f"错误: {exc.name}"}
>     )
> ```

---

### 11.10 部署方式

**回答**：

> FastAPI 应用可以通过多种方式部署。
>
> **uvicorn（推荐）**：
> ```bash
> uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
> ```
>
> **gunicorn + uvicorn workers**：
> ```bash
> gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker
> ```
>
> **Docker 部署**：
> ```dockerfile
> FROM python:3.11
> WORKDIR /app
> COPY requirements.txt .
> RUN pip install -r requirements.txt
> COPY . .
> CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
> ```
>
> **Docker Compose**：
> ```yaml
> services:
>   api:
>     build: .
>     ports:
>       - "8000:8000"
>     depends_on:
>       - db
>   db:
>     image: postgres:15
> ```

---

## 十二、速记表

### Java 并发

| 关键字/类 | 作用 |
|-----------|------|
| synchronized | 内置锁，可重入，非公平 |
| volatile | 可见性，禁止重排序 |
| CAS | 比较交换，原子操作 |
| AQS | JUC 基础，队列同步器 |
| ThreadLocal | 线程本地变量 |
| ThreadPoolExecutor | 线程池 |

### Redis

| 问题 | 解决方案 |
|------|----------|
| 缓存穿透 | 布隆过滤器、缓存空值 |
| 缓存击穿 | 互斥锁、永不过期 |
| 缓存雪崩 | 过期时间+随机、热点不过期 |
| 分布式锁 | SETNX + Lua / Redisson |

### MySQL

| 概念 | 说明 |
|------|------|
| 聚簇索引 | 数据和索引在一起（InnoDB 主键索引） |
| 回表 | 查普通索引拿到主键，再查主键索引 |
| MVCC | 多版本并发控制，快照读 |
| 隔离级别 | RC/RU/RR/SERIALIZABLE |

### 微服务

| 概念 | 说明 |
|------|------|
| CAP | 一致性、可用性、分区容恕 |
| Nacos | 注册中心+配置中心，支持CP/AP |
| Feign | 声明式HTTP客户端 |
| Sentinel | 熔断降级，流量控制 |

---

## 十三、大模型 / AI 应用

### 13.1 怎么评估 RAG 系统

**回答**：

> RAG 系统的评估通常从**检索质量**和**生成质量**两个维度展开。
>
> **检索质量指标**：
> - **Recall@K**：检索结果中包含正确答案的比例
> - **MRR@K**（Mean Reciprocal Rank）：第一个正确答案排名的倒数
> - **Hit@K**：前K个结果中命中的比例
> - **NDCG@K**：考虑排序质量的指标
>
> **生成质量指标**：
> - **Answer Accuracy**：答案是否正确回答问题
> - **Answer Relevance**：答案和问题相关程度
> - **Faithfulness**：答案是否忠实于检索到的上下文（不 hallucinate）
> - **Answer Completeness**：答案是否完整
>
> **常用评估框架**：
> - **RAGAS**（RAG Assessment）：专门评估 RAG 系统的开源框架
> - **TruLens**：提供 Groundedness、Answer Relevance 等指标
> - **LLM as Judge**：用大模型评判答案质量（用更强的模型如 GPT-4 评 GPT-3.5 的回答）
>
> **如果专门问 Ragas 怎么评估**：
> - **Ragas 是什么**：Ragas 是一个专门评估 RAG 系统的框架，不仅能评估检索效果，也能评估生成质量。它的好处是能把问题拆开看，不只是盯着最终答案。
> - **输入（评估数据集）**：一个标准评测样本通常包含 4 个字段：
>   1. `question`：用户问题
>   2. `answer`：系统生成的答案
>   3. `contexts`：检索召回的上下文
>   4. `ground_truth`：人工标注的标准答案或标准事实
> - **输出（评估指标）**：
>   1. **Faithfulness（忠诚度）**：衡量生成答案和给定上下文之间的一致性，通常是 0 到 1。核心就是看大模型是不是根据上下文回答，而不是脱离上下文自己发挥。
>   2. **Answer Relevance（答案相关性）**：衡量答案和 query 的相关性，看它有没有真正回答用户问题。
>   3. **Context Precision（上下文精确度）**：衡量召回出来的上下文里，相关的真实信息是不是排在比较靠前的位置，也可以理解成召回上下文和问题的匹配纯度。
>   4. **Context Recall（上下文召回率）**：衡量回答问题所需要的关键信息，有多少已经被召回到了上下文里。尤其一个 query 里不止一个子问题时，这个指标更重要。
>
> **我们的实践**：
> - 构建评估数据集（`question + answer + contexts + ground_truth`）
> - 定期跑评估流程，监控各指标趋势
> - 用 Bad Case 分析定位问题（是检索问题还是生成问题）

**面试回答模板**：
> 我们评估 RAG，不会只看最后答案对不对，而是会用 Ragas 把它拆成检索和生成两部分来看。输入一般包括 `question`、`answer`、`contexts` 和 `ground_truth`，输出重点看四个指标：Faithfulness、Answer Relevance、Context Precision 和 Context Recall。这样如果结果不好，我能进一步判断到底是上下文没召回全、排序不合理，还是生成阶段没有忠实使用上下文。

**一条评估样本长什么样**：
```json
{
  "question": "鼓山站今天水位是否超过警戒值？",
  "answer": "根据系统记录，鼓山站今天水位没有超过警戒值。",
  "contexts": [
    "鼓山站 2025-08-12 监测水位为 12.3 米，警戒水位为 13.0 米。",
    "当监测水位高于警戒水位时，系统会标记为超警。"
  ],
  "ground_truth": "鼓山站今日水位 12.3 米，未超过 13.0 米警戒值，因此未超警。"
}
```

**怎么给面试官解释这条样本**：
> 这条样本里，`question` 是用户问题，`answer` 是系统真实输出，`contexts` 是检索召回的证据，`ground_truth` 是人工标注的标准答案。评估时不是只看 answer 对不对，而是要看 answer 是否和 contexts 一致、contexts 有没有把该召回的信息召全。

**Ragas 代码怎么跑**：
```python
from datasets import Dataset
from ragas import evaluate
from ragas.metrics import (
    faithfulness,
    answer_relevancy,
    context_precision,
    context_recall,
)

data = {
    "question": [
        "鼓山站今天水位是否超过警戒值？"
    ],
    "answer": [
        "根据系统记录，鼓山站今天水位没有超过警戒值。"
    ],
    "contexts": [[
        "鼓山站 2025-08-12 监测水位为 12.3 米，警戒水位为 13.0 米。",
        "当监测水位高于警戒水位时，系统会标记为超警。"
    ]],
    "ground_truth": [
        "鼓山站今日水位 12.3 米，未超过 13.0 米警戒值，因此未超警。"
    ]
}

dataset = Dataset.from_dict(data)

result = evaluate(
    dataset=dataset,
    metrics=[
        faithfulness,
        answer_relevancy,
        context_precision,
        context_recall,
    ]
)

print(result)
```

**如果面试官问“这段代码你怎么讲”**：
> 这段代码本质上就是把评估样本整理成 Dataset，再交给 Ragas 跑四个指标。最后输出的是每条样本和整体平均分。我在项目里会更关注两个层面，一个是整体趋势，比如优化后平均分有没有提升；另一个是 bad case，就是哪些问题分数特别低，再回到原始 query、检索结果和最终答案逐条分析。

**怎么根据四个指标反推问题在哪**：

**1. Faithfulness 低**
> 说明生成答案和上下文不一致，或者模型脱离证据自己发挥了。通常更像生成阶段问题，比如 Prompt 约束不够、上下文注入方式有问题，或者模型把上下文理解错了。

**2. Answer Relevance 低**
> 说明答案和用户问题相关性不高，可能答偏、答泛，或者只回答了问题的一部分。这通常可能是 Query Rewrite 不准、Prompt 没限制好回答范围，或者多子问题场景下漏答。

**3. Context Precision 低**
> 说明召回的上下文里噪声比较多，不相关内容也被排到了前面。一般更像检索排序问题，可能是 chunk 太大、召回太宽、BM25 和向量融合不合理，或者 rerank 没把最相关证据顶上来。

**4. Context Recall 低**
> 说明回答问题需要的关键信息没有被完整召回。这个通常更像召回问题，可能是 Query Rewrite 没改好、切片太碎或太粗、embedding 不合适，或者 topK 太小导致有用证据没进上下文。

**一个比较完整的排查思路**：
> 如果 `Context Recall` 低，我先看是不是检索没召全；如果 `Context Precision` 低，我重点看召回噪声和排序；如果上下文看起来是对的，但 `Faithfulness` 低，那多半是生成阶段没忠实用上下文；如果 `Answer Relevance` 低，就看是不是答偏了、漏答了，或者 query 本身改写得不准确。也就是说，这四个指标的价值不是单独看分数，而是帮助我快速判断问题更偏检索侧还是生成侧。

---

### 13.2 怎么解决大模型幻觉问题

**回答**：

> 幻觉（Hallucination）是大模型生成看似正确但实际错误内容的问题。解决思路分几个层次：
>
> **1. 输入层：优化 Prompt**
> - 明确指定"不知道就说不知道"，不要胡编
> - 添加"基于以下内容回答"等约束
> - Few-shot 示例中包含"我不知道"的例子
>
> **2. 检索层：提升 RAG 质量**
> - 检索更精准的上下文，减少无关信息干扰
> - 增加重排序（ReRank），把最相关的文档排在前面
> - 对检索结果做置信度过滤，低于阈值的不送生成
>
> **3. 生成层：约束输出**
> - **Retrieval-Augmented Generation (RAG)**：带检索地生成，答案必须有据可查
> - **Chain-of-Verification (CoV)**：生成后让模型自己验证每个陈述
> - **Self-Consistency**：多次生成，取最一致的答案
>
> **4. 模型层：微调或换模型**
> - 对特定领域做 SFT 微调，减少胡编
> - 用 RLHF（基于人类反馈的强化学习）让模型更诚实
> - 换用幻觉率更低的基础模型
>
> **5. 后处理层：事实校验**
> - 用外部知识库（如 Wikipedia API）校验生成的内容
> - 对数值、日期、人名等实体做 Cross-check
>
> **我们的实践**：
> - RAG 场景下，确保检索召回的上下文能支持生成答案，做到"有据可查"
> - 生成时 Prompt 里加"如果找不到答案，回复'暂时无法解答'"这类约束
> - 上线后持续收集 Bad Case，针对性优化

---

### 13.3 RAG 流程详解

**回答**：

> RAG（Retrieval-Augmented Generation）= 检索 + 生成。
>
> **完整流程**：
> ```
> 用户问题 → 编码 → 检索向量库 → 取出 Top-K 相关文档 → 与问题拼接 → LLM 生成答案
> ```
>
> **具体步骤**：
> 1. **文档处理**：PDF/Markdown 等原始文档 → 文本切分（Chunk）→ Embedding 模型编码 → 存入向量数据库
> 2. **Query 编码**：用户问题 → 同一 Embedding 模型编码成向量
> 3. **向量检索**：用 ANN（Approximate Nearest Neighbor）算法（如 HNSW、IVF）从向量库中找最相似的 K 个 Chunk
> 4. **重排序（可选）**：用 Cross-Encoder 对检索结果重新排序，提高精度
> 5. **生成**：把 Top-K Chunk 作为上下文，连同用户问题一起发给 LLM
>
> ** Chunk 策略**：
> - 固定长度切分（如 512 tokens），可能切断语义
> - 语义切分（按句子/段落切），保留完整性
> - 重叠切分（overlap），减少上下文丢失
>
> **Embedding 模型选择**：
> - 中文：BGE、Text2Vec、SimCSE
> - 英文：OpenAI text-embedding-ada-002、Cohere
>
> **向量数据库**：
> - Milvus、Pinecone（云）、Chroma（轻量）、FAISS（Facebook）、Weaviate

---

### 13.4 向量数据库有哪些？各自特点

**回答**：

| 数据库 | 特点 | 适用场景 |
|--------|------|----------|
| **Milvus** | 开源、分布式、支持混合检索 | 大规模生产环境 |
| **Pinecone** | 云原生、易用、托管服务 | 快速上线 |
| **Chroma** | 轻量、嵌入式、Python 原生 | 原型验证、小规模 |
| **FAISS** | Facebook 开源、高性能 | 研究、离线场景 |
| **Weaviate** | 支持 GraphQL、内置向量化 | 知识图谱场景 |
| **Qdrant** | 高性能、支持过滤 | 生产级应用 |

**选型考虑**：
- 数据规模：大规模用 Milvus/Pinecone，小规模用 Chroma
- 是否需要混合检索（向量 + 标量过滤）
- 部署方式：云托管 vs 私有部署
- 性能要求：QPS、延迟

---

### 13.5 什么是向量检索 ANN 算法

**回答**：

> ANN（Approximate Nearest Neighbor）——近似最近邻搜索，在精度和速度之间做权衡。
>
> **常见算法**：
>
> | 算法 | 原理 | 特点 |
> |------|------|------|
> | **HNSW** | 分层 navigable small world 图 | 速度快、内存占用较高 |
> | **IVF** | 倒排索引 + 聚类 | 支持过滤、需预训练 |
> | **PQ** | product quantization 量化压缩 | 内存占用低、精度稍差 |
> | **LSH** |  locality-sensitive hashing | 保证召回、速度慢 |
>
> **HNSW 原理**：
> - 构建多层图，上层稀疏、下层密集
> - 搜索时从上层入口快速定位，再在下层精确搜索
> - 跳表思想：上层快速跳过大部分数据
>
> **衡量指标**：
> - **召回率（Recall）**：找到的近邻占全部近邻的比例
> - **QPS**：每秒查询数
> - **延迟（P99）**：99 分位延迟

---

### 13.6 LLM 推理加速有哪些方法

**回答**：

> **模型层加速**：
> - **量化（Quantization）**：INT8/INT4 量化，减小模型体积和计算量
>   - 动态量化 vs 静态量化
>   - GPTQ、AWQ、GGML 格式
> - **剪枝（Pruning）**：移除不重要的神经元或连接
> - **蒸馏（Distillation）**：用大模型教小模型（如 Llama 7B 蒸馏 3B）
>
> **推理层加速**：
> - **Batching**：批量处理请求，提高 GPU 利用率
> - **KV Cache**：缓存已计算的 Key-Value，避免重复计算
> - **Continuous Batching**：动态批处理，新请求可随时加入
>
> **框架层**：
> - **TensorRT-LLM**：NVIDIA 高性能推理引擎
> - **vLLM**：PagedAttention 管理 KV Cache，吞吐量高
> - **ONNX Runtime**：跨平台高性能推理
> - **llama.cpp**：CPU/GPU 混合推理，支持量化
>
> **我们的实践**：
> - 生产环境用 vLLM 部署，QPS 提升明显
> - 部分场景用 INT4 量化后的 llama.cpp，内存占用减半

---

### 13.7 LangChain 用过吗？有哪些核心组件

**回答**：

> LangChain 是一个 LLM 应用开发框架，核心模块：
>
> **1. Model I/O**：
> - 模型调用封装（OpenAI、Claude、本地模型）
> - Prompt 模板管理
> - 输出解析（JSON Parser、Struct Output）
>
> **2. Retrieval**：
> - 文档加载（PDF、TXT、Markdown）
> - 文本切分（CharacterTextSplitter、RecursiveCharacterTextSplitter）
> - 向量存储（Chroma、FAISS、Milvus）
> - 检索链（Retriever Chain）
>
> **3. Chains**：
> - LLMChain：简单链
> - SequentialChain：顺序执行多个链
> - ConversationalChain：对话链
> - RAG 链：检索 + 生成
>
> **4. Agents**：
> - 根据用户输入动态决定下一步动作
> - Tool：搜索引擎、计算器、数据库查询
> - Agent：根据 Tool 描述自动选择
>
> **5. Memory**：
> - 对话历史管理
> - ConversationBufferMemory、ConversationSummaryMemory
>
> **我们的项目里**：
> - 用 LangChain 的 RAG Chain 封装检索和生成流程
> - 用 LCEL（LangChain Expression Language）定义链式调用

---

### 13.8 大模型微调有哪些方法

**回答**：

> **1. 全量微调（Full Fine-tuning）**
> - 更新所有参数
> - 缺点：算力需求大、容易过拟合、灾难性遗忘
>
> **2. LoRA（Low-Rank Adaptation）**
> - 在原模型旁边加旁路 A、B 矩阵，冻结原参数
> - 只更新 A、B 矩阵，参数量大幅减少
> - 显存需求：原模型 24G + LoRA 几 G
> - 效果接近全量微调
>
> **3. QLoRA（Quantized LoRA）**
> - 4-bit 量化模型 + LoRA
> - 进一步降低显存，RTX 3090 也能微调 65B 模型
>
> **4. Adapter**
> - 在 Transformer 层之间插 Adapter 小模块
> - 参数量比 LoRA 更少
>
> **5. Prompt Tuning / Prefix Tuning**
> - 不改模型权重，只调 Prompt
> - Prompt Tuning：学习虚拟 Token
> - Prefix Tuning：在输入前加可学习的 prefix
>
> **微调数据格式**：
> ```json
> {"instruction": "问题", "input": "上下文", "output": "答案"}
> ```
>
> **我们的实践**：
> - 用 LoRA 对中文模型（如 Qwen、ChatGLM）做领域适配
> - 收集 1K~10K 条领域数据，效果提升明显

---

### 13.8.1 LoRA 微调要调哪些参数

**回答**：

> LoRA 的参数分两大类：**LoRA 本身的核心参数** + **训练通用参数**。

---

**一、LoRA 核心参数（最重要的几个）**

| 参数 | 作用 | 常用值 | 调参建议 |
|------|------|--------|---------|
| **r**（rank） | 旁路矩阵的秩，越大参数量越多，效果越好 | 4/8/16/32/64 | 数据量小用 4~8，数据量大多用 16~64 |
| **lora_alpha** | 缩放系数，配合 r 一起用，通常设成 r 的 2 倍 | 16/32/64/128 | 和 r 同比例放大 |
| **lora_dropout** | Dropout 比例，防止过拟合 | 0~0.1 | 数据量小适当调大 |
| **target_modules** | 哪些层加 LoRA | q_proj, v_proj, k_proj, o_proj, gate_proj, up_proj, down_proj | 全加效果最好，显存不够只加 q/v |

**rank (r) 的影响**：
```
r=4:   效果好但参数量极小   ← 轻量快速验证
r=8:   平衡之选             ← 日常使用
r=16:  效果接近全量微调     ← 效果要求高时
r=64:  几乎等于全量微调     ← 显存够用全量得了
```

**target_modules 要加哪些层**：
```python
# 通常 attention 相关的层效果最好，必加
target_modules = ["q_proj", "v_proj", "k_proj", "o_proj"]

# 如果显存允许，把 FFN 层也加上
target_modules = ["q_proj", "v_proj", "k_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
```

---

**二、训练通用参数**

| 参数 | 作用 | 常用值 |
|------|------|--------|
| **batch_size** | 每步多少条数据 | 1/2/4/8 |
| **learning_rate** | 学习率 | 1e-4 ~ 1e-5，LoRA 不用太大 |
| **num_train_epochs** | 训练多少轮 | 1~10，数据量小可以多轮 |
| **max_grad_norm** | 梯度裁剪，防止梯度爆炸 | 0.5~1.0 |
| **warmup_ratio** | 预热比例，前多少比例数据用来预热 | 0.1~0.3 |
| **weight_decay** | 权重衰减，防止过拟合 | 0~0.01 |
| **logging_steps** | 多少步打印一次日志 | 10~50 |

**学习率特别说明**：
- 全量微调：1e-5 ~ 5e-5
- LoRA：因为只更新旁路矩阵，学习率可以稍大，1e-4 ~ 3e-4
- 学习率过大容易震荡，过小收敛太慢

---

**三、典型配置示例**

**轻量级配置（RTX 3090 跑 7B 模型）**：
```python
lora_config = {
    "r": 8,
    "lora_alpha": 16,
    "lora_dropout": 0.05,
    "target_modules": ["q_proj", "v_proj"],
    "bias": "none",          # 不训练 bias
    "task_type": "CAUSAL_LM"
}

train_config = {
    "per_device_train_batch_size": 2,
    "gradient_accumulation_steps": 8,   # 累积梯度，等效 batch_size=16
    "learning_rate": 1e-4,
    "num_train_epochs": 3,
    "max_grad_norm": 1.0,
    "warmup_ratio": 0.1,
    "logging_steps": 10
}
```

**效果好的配置（A100 跑 13B 模型）**：
```python
lora_config = {
    "r": 16,
    "lora_alpha": 32,
    "lora_dropout": 0.05,
    "target_modules": ["q_proj", "v_proj", "k_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
    "bias": "none",
    "task_type": "CAUSAL_LM"
}

train_config = {
    "per_device_train_batch_size": 4,
    "gradient_accumulation_steps": 4,
    "learning_rate": 2e-4,
    "num_train_epochs": 3,
    "max_grad_norm": 0.5,
    "warmup_ratio": 0.1,
    "logging_steps": 10,
    "save_steps": 100            # 每100步保存一次
}
```

---

**四、调参优先级**

1. **首先调 r 和 lora_alpha**（最影响效果）
2. **其次调 learning_rate**（第二重要）
3. **最后调 dropout 和 batch_size**（微调用）

---

**五、实战中遇到的问题**

| 问题 | 原因 | 解决 |
|------|------|------|
| 训练 loss 不降 | 学习率太小 or r 太小 | 调大学习率或加大 r |
| 训练 loss 爆炸 | 学习率太大 | 调小 learning_rate，加 gradient clipping |
| 效果不如全量微调 | r 太小 | 调大 r（16 以上） |
| 过拟合严重 | 数据太少 or epoch 太多 | 加 dropout，增数据，早停 |

---

**面试回答**：

> LoRA 核心参数就三个：rank(r)、lora_alpha、target_modules。
>
> r 决定旁路矩阵的维度，越大效果越好但参数量越多，我一般从 8 或 16 开始试。lora_alpha 是缩放系数，通常设成 r 的 2 倍。target_modules 决定在哪些层加 LoRA，attention 的 q 和 v 投影是最关键的。
>
> 训练参数里 learning_rate 最关键，LoRA 因为只更新旁路，不用太大，一般 1e-4~3e-4。batch_size 往大了加就行，梯度累积可以弥补显存不足。

---

### 13.9 什么是 LangChain LCEL

**回答**：

> LCEL（LangChain Expression Language）是 LangChain 定义的链式调用语法。
>
> **核心思想**：用 `|` 操作符把各个组件串起来。
>
> **示例**：
> ```python
> from langchain_core.output_parsers import StrOutputParser
> from langchain_core.prompts import ChatPromptTemplate
> from langchain_openai import ChatOpenAI
>
> # 定义 Prompt
> prompt = ChatPromptTemplate.from_messages([
>     ("system", "你是{language}翻译专家"),
>     ("human", "把这句话翻译成{language}：{text}")
> ])
>
> # 定义模型
> model = ChatOpenAI(model="gpt-3.5-turbo")
>
> # 定义输出解析器
> parser = StrOutputParser()
>
> # 串成链
> chain = prompt | model | parser
>
> # 调用
> result = chain.invoke({"language": "英文", "text": "你好世界"})
> # 等价于：prompt.format() → model.invoke() → parser.invoke()
> ```
>
> **优势**：
> - 代码简洁，链式调用清晰
> - 支持 `.stream()`、`.batch()` 自动切换流式/批量
> - 方便调试：`chain.steps` 查看每一步
> - 可并行：`RunnableParallel` 并行执行多个任务

---

### 13.10 什么是 Function Calling / Tool Use

**回答**：

> Function Calling（大模型工具调用）让 LLM 能够调用外部工具/函数。
>
> **原理**：
> 1. 定义工具的 JSON Schema（描述函数名、参数、用途）
> 2. 用户提问时，模型判断是否需要调用工具
> 3. 如果需要，模型返回符合 Schema 的调用参数
> 4. 代码执行工具，把结果返回给模型
> 5. 模型综合结果生成最终回答
>
> **示例**：
> ```python
> # 1. 定义工具
> tools = [{
>     "type": "function",
>     "function": {
>         "name": "get_weather",
>         "description": "查询城市天气",
>         "parameters": {
>             "type": "object",
>             "properties": {
>                 "city": {"type": "string", "description": "城市名称"}
>             }
>         }
>     }
> }]
>
> # 2. 模型判断调用
> response = client.chat.completions.create(
>     model="gpt-3.5-turbo",
>     messages=[{"role": "user", "content": "北京今天天气怎么样？"}],
>     tools=tools,
>     tool_choice="auto"
> )
>
> # 3. 获取工具调用结果
> # response.choices[0].message.tool_calls[0].function.arguments
> # {"city": "北京"}
>
> # 4. 执行工具，返回结果
> weather = get_weather("北京")
>
> # 5. 把结果发回模型，生成最终回答
> ```
>
> **应用场景**：
> - 查询实时信息（天气、股价、搜索）
> - 调用内部 API（查数据库、查订单）
> - 执行计算（计算器、代码执行）
>
> **我们的项目里**：实现了一个 ToolManager，用 Function Calling 调用内部订单查询、用户验证等接口。

---

### 13.11 什么是向量 Embedding，常见模型有哪些

**回答**：

> Embedding 是把文本/图片等数据映射到向量空间的技术，让语义相似的内容在向量空间中距离近。
>
> **文本 Embedding 模型**：
> | 模型 | 特点 |
> |------|------|
> | **BGE**（BAAI） | 中文效果最好，支持多语言 |
> | **M3E**（Moka） | 中文，轻量级 |
> | **Text2Vec** | 中文，基于 BERT |
> | **Sentence-BERT** | 英文，SNLI 训练的孪生网络 |
> | **text-embedding-ada-002** | OpenAI 官方，通用场景 |
> | **Cohere** | 支持 100+ 语言 |
>
> **Embedding 质量评估**：
> - **MTEB**（Massive Text Embedding Benchmark）：综合评估各模型在分类、聚类、检索等任务上的表现
>
> **选择建议**：
> - 中文场景优先 BGE、M3E
> - 英文场景 OpenAI Embedding、Cohere
> - 要部署到本地用开源，要效果好用 API

---

### 13.13 PDF 解析效果不好怎么解决

**回答**：

> PDF 解析是 RAG 系统的老大难问题，效果不好主要体现在这几个方面：
>
> **常见问题**：
> 1. **表格解析乱码**：PDF 里的表格被解析成一堆乱字符
> 2. **图片/公式无法解析**：扫描件图片、LaTeX 公式直接丢内容
> 3. **阅读顺序错乱**：多栏布局、页眉页脚干扰解析
> 4. **段落被切断**：一句话被拆到两个 Chunk 里
>
> **解决方案**：
>
> **1. 选型层面：换解析库**
> - `PyMuPDF`（fitz）：速度快，对中文支持较好
> - `pdfminer`：解析准确，但慢
> - `paddleocr`：专门处理扫描件、图片类 PDF
> - `marker`（GitHub 开源）：深度学习方案，端到端转 Markdown，效果好但慢
>
> **2. 预处理层面：先转格式再解析**
> - PDF → Markdown/HTML → 解析（有些库解析 HTML 比 PDF 容易）
> - 用 `unstructured` 库自动检测并处理不同类型内容
>
> **3. 表格处理：专用解析器**
> - 通用解析表格效果差，用 `tabula`、`camelot`、`df2xl` 等专用表格解析
> - 或者是 `ByteSpider/unstructured` 等支持表格识别的库
>
> **4. 后处理层面：清洗和修复**
> - 去除页眉页脚（固定位置、重复内容）
> - 去除乱码和特殊字符
> - 合并被切断的段落（按句子/语义切分，而不是固定长度）
>
> **5. 语义切分：减少 Chunk 被切断**
> - 不要固定 512 tokens 切分
> - 按段落、语义切分，保留完整句子
> - overlap 覆盖 10%~20%，减少上下文丢失
>
> **我们的实践**：
> - 线上跑了两个方案：`marker`（效果好但贵）+ `PyMuPDF` 降级（对纯文字类 PDF 够用）
> - 表格类 PDF 用 `camelot` 解析，结果用 JSON 存储而非纯文本
> - 切分策略改用 `RecursiveCharacterTextSplitter`（按段落切），比固定长度切分召回率提升明显

---

### 13.14 知识库更新怎么做

**前置知识：Chunk 和 Document 的关系**

- **Document（文档）**：一个 PDF 文件，就是一个 Document
- **Chunk（块）**：一个 Document 切成多个小段，每段生成一个向量

存入向量库时，每条记录的结构大概是：
```
id: "doc_001_chunk_0"    ← 唯一标识，格式：文档ID + 序号
vector: [0.1, 0.3, ...]  ← 768维向量
metadata: {              ← 额外信息，方便删改查
    "doc_id": "doc_001",      ← 属于哪个文档
    "doc_name": "产品手册.pdf",
    "page": 1,
    "chunk_index": 0
}
```

为什么要记录 doc_id？因为删改的时候是按 doc_id 操作的。

---

**一、从上传文件开始讲**

**用户操作**：用户打开知识库管理页面上传文件，比如"操作手册.pdf"

**上传流程**：

```
用户   →   API接口   →   OSS存储   →   解析   →   入库
         /upload      保存文件     切分      Milvus
                      生成doc_id            存向量
                      写MongoDB元数据
```

**Step 1：用户上传文件**

用户通过 HTTP POST 上传文件，携带文件名：
```python
# 用户请求
POST /api/knowledge/upload
Content-Type: multipart/form-data

file: [操作手册.pdf]
```

**Step 2：生成 doc_id**

有两种常见做法：

| 做法 | 怎么做 | 更新时怎么识别同一个文档 |
|------|--------|----------------------|
| **方案A：doc_id = 文件名** | 直接用文件名做ID，如 `"操作手册.pdf"` | 文件名相同就是同一文档 |
| **方案B：doc_id = UUID** | 系统生成一个 UUID | 存一份 filename→doc_id 映射表 |

**我们用方案A（简单直接）**：
```python
doc_id = file.filename   # "操作手册.pdf"

# 但要处理文件名重复的情况（比如不同目录有同名文件）
# 所以实际用 doc_id = f"{file_path}_{file_hash}"  或者直接存完整路径
doc_id = f"/docs/产品线A/操作手册.pdf"  # 用文件路径区分同名文件
```

**Step 3：文件存 OSS**

```python
# 上传到 OSS（对象存储）
oss_path = f"knowledge_base/{doc_id}.pdf"
oss.put_object(oss_path, file.content)

# 同时记录原始文件名（展示用）
original_filename = file.filename  # "操作手册.pdf"
```

**Step 4：写入 MongoDB 元数据**

```python
# 生成档案记录
mongo.doc_metadata.insert({
    "doc_id": doc_id,                    # "操作手册.pdf" 或 UUID
    "original_filename": "操作手册.pdf",  # 用户看到的文件名
    "oss_path": oss_path,                # OSS存储路径
    "file_hash": md5(file.content),       # 文件内容的MD5
    "status": "pending",                 # pending=待处理，synced=已入库
    "uploaded_at": datetime.now(),
    "chunk_ids": [],                     # 等处理完再填
    "chunk_count": 0
})
```

**Step 5：触发解析流程（异步）**

```python
# 发个消息给消息队列，触发解析
mq.send("knowledge_parse_task", {
    "doc_id": doc_id,
    "oss_path": oss_path,
    "task_id": generate_task_id()
})

# 用户请求立刻返回 "上传成功，正在处理..."
return {"code": 0, "message": "上传成功", "doc_id": doc_id}
```

**Step 6：后台解析（消费者处理）**

```python
# 消费者收到消息，开始处理
def process_document(doc_id, oss_path):
    # 1. 从 OSS 下载文件
    content = oss.get_object(oss_path)

    # 2. 解析PDF
    text = parse_pdf(content)

    # 3. 切分
    chunks = chunk_text(text)  # 比如切成 ["chunk_0", "chunk_1", "chunk_2"...]

    # 4. 转向量
    vectors = embedding_model.encode(chunks)

    # 5. 存入 Milvus
    chunk_ids = []
    for i, (chunk, vec) in enumerate(zip(chunks, vectors)):
        chunk_id = f"{doc_id}_chunk_{i}"
        milvus.insert("knowledge_collection", {
            "id": chunk_id,
            "vector": vec,
            "metadata": {
                "doc_id": doc_id,
                "chunk_index": i,
                "original_filename": original_filename
            }
        })
        chunk_ids.append(chunk_id)

    # 6. 更新 MongoDB 状态
    mongo.doc_metadata.update(
        {"doc_id": doc_id},
        {
            "status": "synced",
            "chunk_ids": chunk_ids,
            "chunk_count": len(chunks),
            "synced_at": datetime.now()
        }
    )
```

**解析完成后**，用户就能在知识库里搜到这份文档了。

---

**二、上传新版本（更新文档）**

**用户操作**：用户上传了新的"操作手册.pdf"（内容变了）

**核心问题**：系统怎么知道这是"更新"而不是"新文档"？

**识别逻辑**：

| 方案 | 做法 |
|------|------|
| **文件名匹配** | 文件名相同 = 同一文档，直接更新（我们用的） |
| **用户主动选择** | 界面上选"更新已有文档"，选择要更新的文档 |
| **文件内容匹配** | MD5 相同 = 没变，跳过；MD5 不同 = 内容变了，要更新 |

**我们用"文件名匹配"方案**（最简单，符合用户直觉）：

```python
def handle_upload(file):
    doc_id = file.filename  # 用户上传什么文件名，就用什么文件名做ID

    # 1. 查 MongoDB，看这个文件名是否已存在
    existing = mongo.doc_metadata.find_one({"doc_id": doc_id})

    if existing:
        # 已存在 = 这是更新操作
        # 先删旧数据（向量库 + 档案）
        delete_document(existing["doc_id"])

        # 再插入新数据（走完整上传流程）
        insert_document(file)

        return {"message": "文档已更新", "doc_id": doc_id}
    else:
        # 不存在 = 新增
        insert_document(file)
        return {"message": "文档已上传", "doc_id": doc_id}
```

---

**三、更新时怎么删掉旧的 Chunk**

```python
def delete_document(doc_id):
    # 1. 按 doc_id 删向量库里的所有向量
    # Milvus 支持按 expression 批量删除
    milvus.delete(
        collection_name="knowledge_collection",
        expr=f'doc_id == "{doc_id}"'
    )
    # 注意：删除是异步的，不是立刻物理删除，但查询时会自动过滤

    # 2. 删 MongoDB 档案
    mongo.doc_metadata.delete({"doc_id": doc_id})

    # 3. 可选：删 OSS 文件
    oss.delete_object(doc_id)
```

---

**四、完整流程图**

```
首次上传"操作手册.pdf"：
用户 → 上传文件 → 生成doc_id="操作手册.pdf" → 存OSS → 写MongoDB(pending)
                                                    ↓ 异步
                                               解析→切分→Embedding→入库
                                                    ↓
                                               更新MongoDB(synced)

再次上传"操作手册.pdf"（新版本）：
用户 → 上传文件 → doc_id="操作手册.pdf" → 查MongoDB发现已存在
                                          ↓
                                    先删旧向量（按doc_id删）
                                    再重新走解析入库流程
```

---

**面试回答**：

> 从上传讲起。用户上传 PDF 到 API，API 把文件存 OSS，然后用一个任务异步处理：PDF 解析 → 文本切分 → 向量化 → 存入 Milvus。每条向量记录里带一个 doc_id 字段，MongoDB 也存了一份档案记录 doc_id、文件路径、MD5 值等。
>
> 用户上传新版本时，如果文件名和之前相同，系统就认为是更新操作：先按 doc_id 删掉 Milvus 里这个文档的所有向量，再重新解析入库。MongoDB 档案也是先删后插。
>
> 如果文件名是个新名字，就走新增流程。

---

### 13.15 Milvus 热更新了解吗

**前置知识：Milvus 的 Collection 是什么**

打个比方：
- **MySQL** 里：Database → Table → Row
- **Milvus** 里：Database → Collection → Vector

Collection 就是存放向量的表，每个向量有一个 ID 和 vector 字段。

---

**热更新是什么**

热更新 = **不停服更新数据**。

对应的词是"冷更新"，冷更新需要停机（比如删表重建期间服务不可用）。

---

**Milvus 哪些操作支持热更新（不停服）**

| 操作 | 能否热更新 | 说明 |
|------|----------|------|
| Insert（插入新向量） | ✅ | 不影响查询，直接写入 |
| Delete（按 ID 删除） | ✅ | 删除后查询自动过滤，不影响正在跑的查询 |
| Upsert（存在就更新，不存在就插入） | ✅ | Milvus 2.3+ 支持 |
| 改变副本数 | ✅ | 不影响服务 |
| 建新 Index（异步） | ✅ | 后台建Index，建完自动生效，不影响查询 |
| 切换 Index 类型（HNSW→IVF） | ❌ | 要重建，但可以用 alias 切换实现零停机 |
| 修改 Schema（加字段/删字段） | ❌ | 必须重建 Collection |
| 改变分片键（shard_key） | ❌ | 必须重建 Collection |

---

**核心问题：Schema 变了怎么办？**

比如你一开始存的向量是 768 维，现在模型换成 1024 维了，Schema 不兼容。

**用 Alias 实现零停机切换**：

核心思路：业务不直接连 Collection 名，而是连 Alias（别名）。切换时只需要把 Alias 指向新的 Collection。

```
切换前：
  Alias "my_docs" → Collection "docs_v1"

切换后：
  Alias "my_docs" → Collection "docs_v2"   ← 新 Collection 结构不同，但 alias 不变
```

**完整流程**：
```
# 1. 建新 Collection（新 Schema / 新 Index / 新数据）
create collection "docs_v2" with schema {fields: [...]}

# 2. 对新 Collection 建 Index、导数据
create index on "docs_v2" ...
insert into "docs_v2" ...

# 3. 把 Alias 从旧 Collection 切到新 Collection（毫秒级，原子操作）
create alias "my_docs" for collection "docs_v2"

# 4. 确认切换成功后，可以删掉旧 Collection
drop collection "docs_v1"
```

**为什么能零停机**：
- 步骤3是原子操作，alias 切换瞬间完成
- 业务代码连的是 alias `my_docs`，不感知底层是哪个 Collection
- 步骤4是事后清理，不影响服务

**Milvus alias 相关命令**：
```python
# Python SDK 写法
from pymilvus import connections, Collection, FieldSchema, CollectionSchema, DataType, utility

# 建新 Collection（假设换了新的向量维度）
new_fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True),
    FieldSchema(name="vector", dtype=DataType.FLOAT_VECTOR, dim=1024)  # 维度从768变成1024
]
new_schema = CollectionSchema(fields=new_fields)
new_collection = Collection(name="docs_v2", schema=new_schema)

# 对新 Collection 建 Index
new_collection.create_index(field_name="vector", index_params={"index_type": "HNSW", "params": {"M": 16, "efConstruction": 200}})

# 往新 Collection 导入新数据...

# 原子切换 alias
utility.create_alias(collection_name="docs_v2", alias="my_docs")

# 切完后，旧 Collection 可以删了
# 如果旧 Collection 还在，必须等所有正在进行的查询结束才行
```

---

**面试回答**：

> Milvus 热更新主要看什么操作。
>
> 插入、删除、upsert 这些都支持热更新，不影响服务。建 Index 也是异步的，建完自动生效，不影响查询。
>
> 但如果 Schema 变了（比如向量维度变了），就必须重建 Collection。这种情况用 alias 切换实现零停机：
>
> 先建一个新 Collection，把新数据导入新 Collection，然后用一个 alias 从旧 Collection 切到新 Collection。Alias 切换是原子操作，毫秒级完成，业务不感知。切完后旧 Collection 择机删掉。
>
> 我线上用的就是这个策略，知识库更新时新 Collection 准备好后 alias 一切换，用户无感知。

---

### 13.16 速记表：大模型

| 概念 | 说明 |
|------|------|
| RAG | 检索增强生成，解决幻觉、知识过时 |
| LoRA | 低秩适配微调，只更新旁路矩阵 |
| HNSW | 向量检索 ANN 算法，图结构 |
| LangChain | LLM 应用开发框架 |
| LCEL | LangChain 表达式语言，链式调用 |
| Function Calling | 让模型调用外部工具 |
| Embedding | 把文本映射成向量 |
| Vector DB | 存储向量的数据库（Milvus/Pinecone/Chroma） |
| 幻觉 | 模型生成看似正确但实际错误的内容 |
| Token | 文本拆分的基本单位，约等于 0.75 个英文单词 |
| Milvus alias | 用于 Collection 热切换，不影响业务连接 |
---

### 13.18 DeepSeek 的混合注意力（MLA）是什么

**前置知识：标准 Multi-Head Attention 的问题**

标准 MHA（Multi-Head Attention）每个头有独立的 Q、K、V：

```
每个头都有独立的 Wq, Wk, Wv 矩阵
```

问题：显存占用大，K、V 是 O(n) 增长的，长上下文时显存爆炸。

---

**MLA（Multi-Head Latent Attention）是什么**

MLA 是 DeepSeek-V2 提出的创新注意力机制，核心思想是**低秩投影 + 共享 KV**。

**核心改动**：

| 标准 MHA | MLA |
|---------|-----|
| 每个头独立 Q、K、V | 对 K、V 做低秩压缩（共用） |
| 显存放 Q/K/V 三个矩阵 | 只存压缩后的 latent vector |
| Q 保留完整维度 | Q 做正常多头 |

**原理图**：
```
标准 MHA：
Q → [Wq] → Q1, Q2, Q3... (每个头独立)
K → [Wk] → K1, K2, K3... (每个头独立)
V → [Wv] → V1, V2, V3... (每个头独立)

MLA：
K → [Wk] → 压缩成 [Wk_down] → 低维 K'
V → [Wv] → 压缩成 [Wv_down] → 低维 V'
（所有头共用压缩后的 K', V'，节省显存）

Q 保持正常多头（[Wq] → Q1, Q2, Q3...）
```

**显存节省原理**：
- 标准 MHA：每个 token 的 KV cache 是 O(n × d × h)（n=序列长，d=维度，h=头数）
- MLA：KV 只存低维压缩向量，显存降为 O(n × d' × h)（d' << d）

**MLA 公式**：
```
K^C = W^{DK} × h_K          # K 压缩（低秩投影）
V^C = W^{DV} × h_V          # V 压缩（低秩投影）
h_K, h_V 是 latent vector  # 比原始 K/V 维度低很多

输出 = attention(Q, K^C, V^C)
```

---

**MLA vs MHA vs GQA 对比**

| 机制 | 特点 | 显存 |
|------|------|------|
| **MHA（标准）** | 每个头独立 Q/K/V | 最高 |
| **GQA** | 所有头共用 K/V（分组） | 中等 |
| **MLA（DeepSeek）** | KV 低秩压缩，所有头共用压缩后的 KV | 最低 |

---

**DeepSeek-V2 的实际配置**（MLA + SwiGLU）：

```python
# DeepSeek-V2 核心配置
"hidden_size": 5120,
"num_attention_heads": 128,
"num_key_value_heads": 16,          # 注意：这里不是 GQA
"qk_lora_rank": 1536,               # QK 的低秩维度
"v_head_dim": 128,
"kv_lora_rank": 512                 # KV 的低秩压缩维度
```

注意：DeepSeek-V2 不是标准的 GQA，而是一种更激进的 KV 压缩方案。

---

**MLA 的好处**

1. **省显存**：KV cache 大幅减小，可以跑更长的上下文
2. **保持效果**：虽然压缩了，但通过低秩投影保留了关键信息
3. **兼容长上下文**：128K + 的上下文不再受显存限制

---

**面试回答**：

> DeepSeek-V2 引入了 MLA（Multi-Head Latent Attention），核心是对 KV 做低秩压缩。
>
> 标准 MHA 每个头都有独立的 K 和 V，显存消耗大。MLA 把所有头的 K 压缩成一个低维向量，V 也压缩成一个低维向量，所有头共用压缩后的 KV。
>
> 这样 KV cache 从 O(n × d × h) 变成 O(n × d' )，d' << d × h，显存大幅下降，但效果基本不掉。
>
> 这也是 DeepSeek 能支持 128K 超长上下文的技术原因之一。

---

### 13.17 什么是 A2A（Agent-to-Agent）协议

**前置知识：为什么需要 A2A**

现在的 AI 应用越来越复杂，一个 Agent 往往完不成所有事情，需要多个 Agent 协作。

比如一个"旅行助手"Agent，它需要：
- 查天气的 Agent
- 订酒店的 Agent
- 订机票的 Agent
- 写邮件通知的 Agent

这些 Agent 可能来自不同团队、不同公司、甚至用不同的模型，怎么让它们互相通信？

**A2A 就是解决这个问题的协议**。

---

**A2A 是什么**

A2A（Agent-to-Agent）是 Anthropic 在 2025 年提出的开放协议，**让不同的 AI Agent 能够互相通信、协作完成任务**。

类比：
- **A2A ≈ HTTP**，但 HTTP 是给人用的（人→服务器→人）
- **A2A 是给 Agent 用的**（Agent→Agent→Agent）
- 就像 Web 服务用 HTTP 协议通信，Agent 用 A2A 协议通信

**对比 MCP（Model Context Protocol）**：
| 协议 | 作用 | 谁→谁 |
|------|------|-------|
| **MCP** | 让 LLM 调用工具（搜索、代码执行、数据库） | LLM → 工具 |
| **A2A** | 让 Agent 之间互相协作 | Agent → Agent |

可以同时用：
- Agent 通过 **MCP** 调用工具
- Agent 通过 **A2A** 和其他 Agent 通信

---

**A2A 的核心概念**

```
Agent Client（发起方）          Agent Server（接收方）
      │                              │
      │──── Task ──────────────────→│  Agent 描述自己的能力
      │←─── Task ────────────────────│  返回结果（同步/异步）
      │                              │
```

**4 个关键概念**：

| 概念 | 作用 |
|------|------|
| **Agent Card** | Agent 的"名片"，描述自己的能力、端口、协议版本。用 JSON 暴露在 `/.well-known/agent.json` |
| **Task** | 一次协作任务，有唯一 ID，有状态（pending/submitted/working/completed/failed） |
| **Message** | 任务里的一条消息，有 role（user/agent）和 content |
| **Artifact** | Agent 产生的输出物，比如生成的文档、代码、文件 |

---

**A2A 的通信模式**

| 模式 | 适用场景 | 说明 |
|------|---------|------|
| **同步** | 简单查询，快速得到结果 | 发请求，等响应，类似 HTTP REST |
| **异步 / SSE** | 复杂任务，耗时操作 | 发请求后立刻返回 Task ID，用 SSE 流式接收进度和结果 |

---

**怎么实现一个 A2A Agent**

**Step 1：暴露 Agent Card**

定义这个 Agent 能做什么：

```json
// /.well-known/agent.json
{
  "name": "weather-agent",
  "description": "查询全球城市天气预报",
  "url": "http://localhost:8000",
  "version": "1.0.0",
  "capabilities": {
    "streaming": true,
    "pushNotifications": true
  },
  "skills": [
    {
      "id": "get_weather",
      "name": "查询天气",
      "description": "输入城市名，返回天气预报",
      "tags": ["weather", "forecast"]
    }
  ]
}
```

**Step 2：实现 A2A 服务端（FastAPI）**

```python
from fastapi import FastAPI, SSE
from fastapi.middleware.cors import CORSMiddleware
import sse

app = FastAPI()
app.add_middleware(CORSMiddleware, allow_origins=["*"])

# 1. 提供 Agent Card
@app.get("/.well-known/agent.json")
async def get_agent_card():
    return {
        "name": "weather-agent",
        "description": "查询全球城市天气预报",
        "url": "http://localhost:8000",
        "version": "1.0.0",
        "capabilities": {"streaming": True, "pushNotifications": True},
        "skills": [
            {"id": "get_weather", "name": "查询天气", "description": "输入城市名，返回天气预报"}
        ]
    }

# 2. 处理任务提交（核心接口）
@app.post("/tasks/send")
async def send_task(task: dict):
    """
    接收其他 Agent 发来的任务
    task 格式：
    {
        "taskId": "uuid",
        "messages": [{"role": "user", "content": "北京明天天气怎么样"}],
        "skill": "get_weather"
    }
    """
    # 解析用户意图
    user_message = task["messages"][-1]["content"]

    # 调用实际能力（这里调用天气 API）
    weather_info = call_weather_api(user_message)

    # 返回结果
    return {
        "taskId": task["taskId"],
        "status": "completed",
        "artifacts": [{
            "id": "result-1",
            "type": "text",
            "content": weather_info
        }]
    }

# 3. SSE 流式推送（可选，支持流式输出）
@app.get("/tasks/{task_id}/subscribe")
async def subscribe_task(task_id: str):
    async def event_generator():
        # 模拟流式输出
        for chunk in generate_weather_stream(task_id):
            yield f"data: {chunk}\n\n"

    return SSE(event_generator())
```

**Step 3：实现 A2A 客户端（调用其他 Agent）**

```python
import httpx

class A2AClient:
    def __init__(self, agent_url: str):
        self.agent_url = agent_url

    async def find_agent(self, skill_needed: str):
        """从 Agent Card 里找有对应 skill 的 Agent"""
        async with httpx.AsyncClient() as client:
            card = await client.get(f"{self.agent_url}/.well-known/agent.json")
            for skill in card.json()["skills"]:
                if skill_needed == skill["id"]:
                    return card.json()
        return None

    async def send_task(self, agent_url: str, user_message: str):
        """发送任务给其他 Agent"""
        task_id = "task-" + str(uuid.uuid4())

        async with httpx.AsyncClient() as client:
            response = await client.post(
                f"{agent_url}/tasks/send",
                json={
                    "taskId": task_id,
                    "messages": [{"role": "user", "content": user_message}],
                    "skill": "get_weather"
                }
            )
            return response.json()

# 使用
client = A2AClient("http://localhost:8000")
result = await client.send_task(
    "http://weather-agent:8000",
    "北京明天天气怎么样"
)
print(result["artifacts"][0]["content"])
```

---

**A2A 的实际协作流程**

```
用户：帮我安排下周五去上海的行程

Agent Client（旅行助手）
    │
    ├─→ A2A ─→ Weather Agent：查下周五上海天气
    │         ← A2A ─ {天气：晴，25度}
    │
    ├─→ A2A ─→ Hotel Agent：订上海酒店（预算500-800）
    │         ← A2A ─ {酒店：浦东香格里拉，650/晚}
    │
    ├─→ A2A ─→ Flight Agent：订北京到上海机票
    │         ← A2A ─ {机票：东航MU5101，800/人}
    │
    └─→ 综合结果，生成行程单给用户
```

每个 Agent 独立存在，通过 A2A 互相通信，就像微服务用 HTTP 通信一样。

---

**面试回答**：

> A2A 是 Agent-to-Agent 协议，让不同的 AI Agent 能互相通信、协作完成任务。
>
> 核心是：每个 Agent 暴露一个 Agent Card（JSON），声明自己有哪些能力。其他 Agent 可以发现它，然后通过 `/tasks/send` 发送任务给它。
>
> 我理解它和 MCP 的区别是：MCP 是 LLM 调用工具，A2A 是 Agent 调用 Agent。就像一个是前后端 REST API，一个是微服务之间互相调用。
>
> 我们项目里用 A2A 让负责不同任务的 Agent 协作，比如旅行助手 Agent 调用天气、机票、酒店 Agent。

---

### 13.19 MCP 怎么鉴权

**前置知识：MCP 是什么**

MCP（Model Context Protocol）是 Anthropic 提出的协议，让 LLM 能够调用外部工具。

类比：
- **MCP ≈ REST API**，但更结构化
- LLM 通过 MCP 调用搜索、数据库、文件系统等工具
- 工具服务器暴露自己的能力，LLM 按需调用

---

**MCP 鉴权的核心问题**

MCP 工具服务器通常部署在内网或私有环境，LLM 怎么确认调用者是合法授权的？

---

**MCP 的鉴权方式**

| 方式 | 适用场景 | 原理 |
|------|---------|------|
| **API Key** | 简单场景 | 请求带 `Authorization: Bearer <key>` |
| **OAuth 2.0** | 企业级、多方授权 | 标准 OAuth 流程，Token 有时效 |
| **mcp-oauth 扩展** | MCP 原生支持 | MCP 协议内置了 OAuth 的标准扩展 |
| **自签名 JWT** | 微服务间调用 | 用共享密钥签发 JWT，验证签名 |

---

**MCP 官方推荐的鉴权流程（OAuth 2.0）**

```
Client（LLM应用）           MCP Server（工具服务器）
      │                              │
      │──── 1. 发起授权请求 ─────────→│
      │                              │
      │←─── 2. 重定向到授权页面 ───────│
      │                              │
      │（用户在授权页面确认）          │
      │                              │
      │──── 3. 授权码返回 ───────────→│
      │                              │
      │──── 4. 用授权码换 Access Token→│
      │←─── 5. 返回 Token ────────────│
      │                              │
      │──── 6. 带 Token调用工具 ───────→│
      │←─── 7. 返回结果 ─────────────│
```

---

**代码示例：API Key 鉴权（最常用）**

```python
# MCP Server 端：验证 API Key
from fastapi import FastAPI, Header, HTTPException

app = FastAPI()

API_KEY = "sk-xxx"  # 预共享的 API Key

@app.post("/tools/search")
async def search_tools(
    query: str,
    authorization: str = Header(None)
):
    # 1. 验证 Authorization 头
    if not authorization:
        raise HTTPException(status_code=401, detail="Missing auth")

    # 2. 提取 Bearer Token
    parts = authorization.split()
    if len(parts) != 2 or parts[0] != "Bearer":
        raise HTTPException(status_code=401, detail="Invalid auth format")

    token = parts[1]
    if token != API_KEY:
        raise HTTPException(status_code=401, detail="Invalid token")

    # 3. 鉴权通过，执行工具逻辑
    return {"result": do_search(query)}
```

```python
# MCP Client 端：携带 API Key 调用
import httpx

async def call_mcp_tool(tool_url: str, query: str):
    headers = {
        "Authorization": "Bearer sk-xxx",
        "Content-Type": "application/json"
    }

    async with httpx.AsyncClient() as client:
        response = await client.post(
            tool_url,
            json={"method": "tools/search", "params": {"query": query}},
            headers=headers
        )
        return response.json()
```

---

**代码示例：JWT 鉴权（适合微服务）**

```python
# Server 端验证 JWT
import jwt
from functools import wraps

SECRET_KEY = "shared-secret-key"

def verify_jwt(token: str):
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
        return payload
    except jwt.ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="Token expired")
    except jwt.InvalidTokenError:
        raise HTTPException(status_code=401, detail="Invalid token")

@app.post("/tools/execute")
async def execute_tool(token: str = Header(...), body: dict = None):
    payload = verify_jwt(token)  # 验证通过拿到 payload

    # payload 里可以拿到调用者的身份信息
    caller_id = payload.get("sub")  # 谁在调用

    # 按调用者身份决定能调用哪些工具
    if caller_id not in ALLOWED_CALLERS:
        raise HTTPException(status_code=403, detail="Not allowed")

    return {"result": execute_tool_logic(body)}
```

```python
# Client 端申请 JWT
import jwt
from datetime import datetime, timedelta

def create_jwt(caller_id: str) -> str:
    payload = {
        "sub": caller_id,
        "exp": datetime.utcnow() + timedelta(hours=1),
        "iat": datetime.utcnow()
    }
    return jwt.encode(payload, SECRET_KEY, algorithm="HS256")

# 调用时带 JWT
headers = {"Authorization": f"Bearer {create_jwt('my-llm-app')}"}
```

---

**MCP 工具的权限控制**

除了鉴权，还要做**授权**（能调用哪些工具）：

```python
# 基于 MCP 工具定义的权限控制
TOOL_PERMISSIONS = {
    "my-llm-app": ["search", "weather", "calculator"],      # 只能调这几个
    "admin-app": ["search", "weather", "calculator", "database", "file:write"]
}

@app.post("/tools/execute")
async def execute_tool(tool_name: str, token: str = Header(...)):
    payload = verify_jwt(token)
    caller_id = payload.get("sub")

    # 检查是否有权限调用这个工具
    allowed = TOOL_PERMISSIONS.get(caller_id, [])
    if tool_name not in allowed:
        raise HTTPException(status_code=403, detail=f"Not allowed to call {tool_name}")

    return {"result": execute(tool_name)}
```

---

**生产环境推荐：OAuth 2.0 + 细粒度权限**

```
1. LLM 应用向授权服务器请求 Token（Client Credentials 模式）
2. 授权服务器验证 LLM 应用的身份，发放 Token
3. LLM 应用带 Token 调用 MCP 工具
4. MCP Server 验证 Token，并检查该应用是否有权限调用这个工具
5. 每次调用都审计日志
```

---

**面试回答**：

> MCP 鉴权最常用的是 API Key 和 JWT。
>
> API Key 适合内部服务间调用，预共享一个 key，每次请求带 `Authorization: Bearer <key>`。
>
> JWT 适合微服务场景，用共享密钥签发，有时效，可以携带调用者身份信息，更安全。
>
> 生产环境推荐 OAuth 2.0，Token 有时效管理，还能做细粒度权限控制 —— 不同 LLM 应用能调用的工具不同。
>
> 除了鉴权，还要做授权：Token 验证通过后，还要检查这个调用者有没有权限调这个工具。

---

### 13.20 Agent 调用工具失败怎么处理

**回答**：

> Agent 调用工具（Function Calling / Tool Use）失败是常见问题，处理思路是：**重试 + 降级 + 兜底返回**。
>
> **1. 重试机制**：
> - 工具调用超时/网络异常时，指数退避重试（1s→2s→4s）
> - 设置最大重试次数（如 3 次），避免无限循环
> - 重试时换个 Agent 实例，避免雪崩
>
> **2. 降级策略**：
> - 工具不可用时，Agent 走降级路径：用内置知识回答，不调用外部工具
> - 或者返回"暂时无法处理，请稍后再试"
> - 关键业务（如订单查询）降级到备用数据源
>
> **3. 兜底返回**：
> - Tool Call 超时后，不返回错误，而是给用户一个友好提示
> - 比如"查询超时，稍后重试"而不是直接报 500
>
> **4. 错误分类处理**：
> | 错误类型 | 处理方式 |
> |---------|---------|
> | 网络超时 | 重试 + 降级 |
> | 工具不存在 | 立即返回错误，不重试 |
> | 参数错误 | 返回参数校验错误信息 |
> | 权限不足 | 返回"无权限"提示 |
> | 工具内部报错 | 记录日志，降级处理 |
>
> **代码示例**（Tool Manager 处理）：
> ```python
> class ToolManager:
>     def __init__(self):
>         self.tools = {}  # tool_name -> tool_func
>
>     async def call_tool(self, tool_name: str, params: dict):
>         if tool_name not in self.tools:
>             return {"error": f"工具 {tool_name} 不存在"}
>
>         for attempt in range(3):
>             try:
>                 result = await self.tools[tool_name](**params)
>                 return {"data": result}
>             except Exception as e:
>                 if attempt == 2:
>                     # 降级：返回友好提示
>                     return {"error": "服务繁忙，请稍后重试"}
>                 await asyncio.sleep(2 ** attempt)  # 指数退避
>
>         return {"error": "服务降级"}
> ```
>
> **我们的项目里**：
> - 用 ToolManager 封装所有工具调用，统一处理超时和重试
> - 工具调用失败率超过 10% 触发告警
> - 关键工具（查订单、查用户）降级到 MySQL 直接查询，不走 Redis/MQ

**面试官继续追问时可以这么答**：
> 我处理工具调用失败，一般不是只说“重试一下”，而是会分层做。先做参数校验，避免无效请求打到下游；调用过程中对超时、网络抖动、临时 5xx 这类可恢复错误做有限次重试；如果重试后还失败，就走降级路径，比如返回已有信息、提示稍后再试，或者切人工；最后把失败原因、入参、重试次数、耗时全部打日志，方便后续排查。

**什么错误会重试，什么错误不会**：
- **可重试**：超时、连接失败、网关错误、短时 5xx
- **不重试**：参数缺失、权限不足、鉴权失败、工具不存在、schema 不匹配

**更贴项目的回答**：
> 在我的项目里，如果知识库、搜索、地图这些工具调用失败，我不会让 Agent 一直重复调用。通常会先判断是不是临时错误，如果是就做 1 到 2 次有限重试；如果不是，或者重试后还失败，就走降级方案，比如只返回当前已有结果，或者明确告诉用户这个外部能力暂时不可用。对于服务站查询、邮件发送这类敏感动作，我还会结合确认机制，避免失败后系统做出错误动作。

---

### 13.20.1 怎么确保结构化输出

**回答**：

> 确保结构化输出，我一般会从三层做约束：**Prompt 约束、Schema 约束、结果校验**。
>
> 这里的 **Schema** 可以理解成“数据结构说明书”或者“输出格式契约”。它规定了返回结果里有哪些字段、每个字段是什么类型、哪些字段必填、哪些字段只能取固定值。对 AI 应用来说，Schema 的作用就是把“模型随便返回一段文本”变成“模型必须按约定格式返回结果”。
>
> **1. Prompt 约束**：
> - 明确要求模型只能输出指定结构
> - 给出字段名、字段类型、字段含义
> - 明确禁止输出额外解释、前后缀、自然语言说明
>
> **2. Schema 约束**：
> - 如果框架支持，我优先用 JSON Schema、Pydantic Model 或函数参数签名来约束输出
> - 让模型不是“尽量输出 JSON”，而是“必须匹配这个结构”
> - 这比只靠 Prompt 更稳
>
> **3. 结果校验**：
> - 程序侧再做一次解析和校验
> - 检查字段是否缺失、类型是否正确、枚举值是否合法
> - 校验失败时做一次修复重试，或者直接走兜底逻辑

**一个简单示例**：
```python
from pydantic import BaseModel
from typing import Literal

class StationQueryResult(BaseModel):
    station_name: str
    status: Literal["normal", "warning", "offline"]
    water_level: float
    need_follow_up: bool
```

> 我的思路是先把目标输出定义成这种结构，再让模型按这个结构返回。拿到结果后，程序再用 Pydantic 校验一次。如果字段缺失、类型错误或者枚举值不合法，这次输出就不能直接进入业务链路。

**什么是 Schema，面试怎么答**：
> Schema 本质上就是一份结构定义。比如我要求模型返回一个站点查询结果，那我会提前定义这个结果必须包含 `station_name`、`status`、`water_level` 这些字段，以及它们各自的类型和约束。这样模型输出就不是自由文本，而是有明确边界的结构化数据。对工程来说，它相当于模型输出和后端程序之间的一份接口契约。

**一个最直观的 Schema Demo**：

**场景**：我要让模型返回“订单查询结果”。

**如果没有 Schema，模型可能会这样返回**：
```text
这个订单已经发货了，预计明天下午送达。
```

> 这句话人能看懂，但程序不好处理。因为后端不知道：
> - 订单号在哪
> - 状态字段叫什么
> - “明天下午”是不是标准时间格式

**如果有 Schema，我会先规定输出结构必须长这样**：
```json
{
  "order_sn": "订单编号",
  "status": "paid/shipped/completed/not_found",
  "eta": "预计送达时间，没有就返回 null"
}
```

**那模型就应该按这个格式返回**：
```json
{
  "order_sn": "ORD20250812001",
  "status": "shipped",
  "eta": "2025-08-13 14:00:00"
}
```

> 这样程序就能稳定处理了，比如：
> - 前端直接展示 `status`
> - 后端根据 `status` 判断下一步要不要提醒用户
> - `eta` 可以直接做时间格式转换

**你可以把 Schema 理解成一个“表单模板”**：
> 模型以前是自由发挥写作文，现在是必须按表单填空。
> 这个表单会提前规定：
> - 要填哪些字段
> - 每个字段填什么类型
> - 哪些字段必填
> - 哪些值只能从固定选项里选

**再看一个 Pydantic 对应的例子**：
```python
from pydantic import BaseModel
from typing import Literal

class OrderStatusResult(BaseModel):
    order_sn: str
    status: Literal["paid", "shipped", "completed", "not_found"]
    eta: str | None = None
```

> 这段代码其实就是在定义 Schema：
> - `order_sn` 必须是字符串
> - `status` 只能是 4 个固定值之一
> - `eta` 可以是字符串，也可以是 `None`

**如果模型返回这种错误结果**：
```json
{
  "order_sn": 123,
  "status": "正在路上",
  "eta": false
}
```

> 那程序校验时就会发现：
> - `order_sn` 应该是字符串，不应该是数字
> - `status` 不在允许的枚举值里
> - `eta` 应该是字符串或空，不应该是布尔值

> 这就是 Schema 的价值：它不只是“规定格式”，还负责帮你拦截不合法输出。

**Structured Output 怎么结合 Pydantic / JSON Schema**：
> 我的做法通常是，先用 Pydantic 定义一个输出模型，因为它在 Python 后端里比较自然；然后如果框架支持，就把这个 Pydantic Model 转成 JSON Schema，直接作为模型输出约束传给大模型。这样前面模型按 Schema 生成，后面程序再用 Pydantic 校验，相当于“生成前约束 + 生成后校验”两层保险。

**Pydantic 示例**：
```python
from pydantic import BaseModel, Field
from typing import Literal

class OrderStatusResult(BaseModel):
    order_sn: str = Field(..., description="订单编号")
    status: Literal["paid", "shipped", "completed", "not_found"]
    eta: str | None = Field(default=None, description="预计送达时间")
```

**如果要转成 JSON Schema**：
```python
schema = OrderStatusResult.model_json_schema()
print(schema)
```

**JSON Schema 大概长这样**：
```json
{
  "type": "object",
  "properties": {
    "order_sn": { "type": "string", "description": "订单编号" },
    "status": {
      "type": "string",
      "enum": ["paid", "shipped", "completed", "not_found"]
    },
    "eta": { "type": ["string", "null"], "description": "预计送达时间" }
  },
  "required": ["order_sn", "status"]
}
```

**怎么把它和大模型结合**：
> 如果是支持结构化输出的框架，我会把这个 Schema 直接传给模型，让模型按这个结构返回；如果框架本身不支持强约束，我至少也会把字段定义和示例写进 Prompt 里，然后拿到结果后再用 Pydantic 解析校验。这样即使模型输出偶尔不稳定，程序层也能把问题挡住。

**面试回答模板**：
> Schema 可以理解成一份数据结构契约，定义了模型输出里有哪些字段、类型是什么、哪些是必填。实际工程里，我通常会先用 Pydantic 定义输出模型，再把它转成 JSON Schema 约束模型生成，最后程序侧再用 Pydantic 校验一次。这样结构化输出不是只靠 Prompt 约束，而是有比较完整的工程保障。

**面试回答模板**：
> 我不会只靠 Prompt 说“请输出 JSON”，因为这样不够稳。我的做法一般是三层：先在 Prompt 里把输出格式说清楚，再用 Pydantic Model、JSON Schema 或函数参数签名做强约束，最后程序侧再做一次解析校验。这样既能保证模型输出的结构稳定，也能保证结果能安全接到后续业务流程里。

**如果面试官追问“为什么结构化输出这么重要”**：
> 因为很多 AI 输出不是给人直接读完就结束了，而是要继续进入下一步程序，比如工具调用、审批流、前端渲染、数据库写入。如果输出结构不稳定，整个链路就会很脆弱。所以结构化输出本质上是在降低模型结果的不确定性，让它更适合工程系统接入。

**如果结构化输出失败了怎么办**：
> 如果解析失败，我一般不会把原始结果直接往下传。通常会先做一次轻量修复，比如让模型根据 schema 重新整理；如果还是不行，就返回兜底结构、提示用户重试，或者人工接管。核心原则是不让不合法的数据进入主链路。

---

### 13.21 MCP 与传统 API 的区别

**回答**：

> MCP（Model Context Protocol）和传统 API 看似都是"提供能力"，但核心区别在于**谁来调用、调用什么、怎么交互**。
>
> | 对比 | 传统 API | MCP |
> |------|---------|-----|
> | **调用者** | 人或程序 | LLM（AI Agent） |
> | **交互方式** | 请求-响应，1:1 | LLM 发现 + 调用，1:N |
> | **发现机制** | 文档/SDK | Agent Card 自动发现 |
> | **参数传递** | 手动拼装 JSON | 模型生成，结构化输出 |
> | **上下文** | 无状态 | 带上下文（Session/Memory） |
> | **错误处理** | HTTP 状态码 | 模型自行判断重试/降级 |
> | **工具组合** | 手动编排 | 模型动态决定调用顺序 |
>
> **1. 调用者不同**：
> - 传统 API：人用 Postman/前端程序调用
> - MCP：LLM 自动发现工具、自动调用，不需要人工介入
>
> **2. 发现机制不同**：
> - 传统 API：看文档，找接口，SDK 包
> - MCP：Agent Card（/.well-known/agent.json）声明能力，LLM 自动发现
>
> **3. 参数生成不同**：
> - 传统 API：前端或后端代码手动拼装参数
> - MCP：模型根据用户意图生成参数（Function Calling）
>
> **4. 交互模式不同**：
> - 传统 API：同步请求-响应
> - MCP：支持同步（SSE）和异步（Task），模型可以流式返回中间结果
>
> **面试回答**：
>
> > MCP 和传统 API 的本质区别是**调用者不同**。
> >
> > 传统 API 是给人用的，人看文档、调接口、处理响应。MCP 是给 AI Agent 用的，Agent 自动发现工具、自动生成参数、自动组合调用。
> >
> > 打个比方：传统 API 是餐厅的点菜系统，顾客自己看菜单点菜。MCP 是智能点餐机器人，客人说"我想吃辣的"，机器人自动发现厨房里哪些菜是辣的、哪些厨师擅长做辣菜，然后自动安排。
> >
> > 所以 MCP 的核心价值是**降低 LLM 调用外部工具的门槛**，让 Agent 能自动发现和组合工具，而不是靠人工写代码对接。

---

### 13.22 什么是 TDD，红绿测试

**回答**：

> TDD（Test-Driven Development）测试驱动开发，先写测试再写业务代码。
>
> **红绿测试**：
> 1. **红**：写一个跑不通的测试（测试你想要的 behavior）
> 2. **绿**：写最少的代码让测试通过
> 3. **重构**：消除重复，优化代码
>
> **循环**：红→绿→重构→红...
>
> ```
> ┌─────────────────────────────────┐
> │  1. 写一个不通过的测试（红）     │
> └──────────────┬──────────────────┘
>                 ↓
> ┌─────────────────────────────────┐
> │  2. 写最少的代码让测试通过（绿）  │
> └──────────────┬──────────────────┘
>                 ↓
> ┌─────────────────────────────────┐
> │  3. 重构，消除重复代码           │
> └──────────────┬──────────────────┘
>                 ↓
>                 └──→ 回到1
> ```
>
> **好处**：
> - 测试先行，保证代码可测试
> - 每次只改一点点，bug 好定位
> - 强迫思考接口设计（先想怎么测，再想怎么写）
> - 有测试网，改代码不慌
>
> **常用框架**：
> - Java：JUnit + Mockito
> - Python：pytest + unittest
> - Go：testing 标准库

---

### 13.23 自己写一个 MCP 要怎么写（Demo）

MCP 核心就三件事：**Agent Card 发现工具** + **工具调用接口** + **SSE 流式返回**。

#### 一、Agent Card（服务发现）

LLM 通过 `/.well-known/agent.json` 自动发现有哪些工具。

```json
// agent_card.json
{
  "name": "demo-server",
  "description": "MCP演示服务器",
  "url": "http://localhost:8000",
  "version": "1.0.0",
  "skills": [
    {
      "id": "get_weather",
      "name": "查询天气",
      "description": "输入城市名，返回天气预报",
      "input": {
        "type": "object",
        "properties": {
          "city": {"type": "string", "description": "城市名称"}
        },
        "required": ["city"]
      }
    },
    {
      "id": "query_order",
      "name": "查询订单",
      "description": "输入订单号，返回订单状态",
      "input": {
        "type": "object",
        "properties": {
          "order_sn": {"type": "string", "description": "订单编号"}
        },
        "required": ["order_sn"]
      }
    }
  ]
}
```

---

#### 二、Server 端（FastAPI）

```python
// main.py
from fastapi import FastAPI, HTTPException
from fastapi.middleware.cors import CORSMiddleware
from pydantic import BaseModel
from typing import Optional, Dict
import json
import asyncio

app = FastAPI(title="MCP Demo Server")
app.add_middleware(CORSMiddleware, allow_origins=["*"])

# 加载 Agent Card
with open("agent_card.json", "r", encoding="utf-8") as f:
    AGENT_CARD = json.load(f)

# 模拟数据库
ORDERS_DB = {
    "ORD20250606001": {"status": "paid", "amount": 299.0},
    "ORD20250606002": {"status": "shipped", "amount": 1299.0},
}

# ============ 工具注册表 ============
class ToolRegistry:
    def __init__(self):
        self.tools: Dict[str, callable] = {}

    def register(self, tool_id: str, func: callable):
        self.tools[tool_id] = func

    async def call(self, tool_id: str, params: dict) -> dict:
        if tool_id not in self.tools:
            return {"error": f"工具 {tool_id} 不存在"}
        try:
            result = await self.tools[tool_id](**params)
            return {"success": True, "data": result}
        except Exception as e:
            return {"success": False, "error": str(e)}

registry = ToolRegistry()

# ============ 工具实现 ============
async def get_weather(city: str) -> dict:
    """模拟天气查询"""
    weather_map = {
        "北京": {"temp": 25, "weather": "晴"},
        "上海": {"temp": 28, "weather": "多云"},
    }
    return weather_map.get(city, {"temp": 20, "weather": "未知"})

async def query_order(order_sn: str) -> dict:
    """模拟订单查询"""
    await asyncio.sleep(0.1)
    if order_sn not in ORDERS_DB:
        raise ValueError(f"订单 {order_sn} 不存在")
    return ORDERS_DB[order_sn]

# 注册工具
registry.register("get_weather", get_weather)
registry.register("query_order", query_order)

# ============ API 接口 ============

@app.get("/.well-known/agent.json")
async def get_agent_card():
    """Agent Card，LLM 自动发现工具"""
    return AGENT_CARD

class ToolCallRequest(BaseModel):
    tool_id: str
    params: dict
    request_id: Optional[str] = None

@app.post("/tools/call")
async def call_tool(req: ToolCallRequest):
    """同步调用工具"""
    result = await registry.call(req.tool_id, req.params)
    return {"request_id": req.request_id, "tool_id": req.tool_id, "result": result}

@app.get("/health")
async def health():
    return {"status": "ok"}
```

---

#### 三、Client 端调用

```python
// client.py
import httpx

async def main():
    async with httpx.AsyncClient() as client:
        # 1. 发现工具（读 Agent Card）
        card = await client.get("http://localhost:8000/.well-known/agent.json")
        print(card.json()["skills"])
        # [{'id': 'get_weather', 'name': '查询天气', ...}, ...]

        # 2. 调用工具
        resp = await client.post(
            "http://localhost:8000/tools/call",
            json={"tool_id": "get_weather", "params": {"city": "北京"}}
        )
        print(resp.json())
        # {"request_id": null, "tool_id": "get_weather",
        #  "result": {"success": True, "data": {"temp": 25, "weather": "晴"}}}

        # 3. 查询订单
        resp2 = await client.post(
            "http://localhost:8000/tools/call",
            json={"tool_id": "query_order", "params": {"order_sn": "ORD20250606001"}}
        )
        print(resp2.json())

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

---

#### 四、SSE 流式返回（可选）

```python
// 流式接口
from sse_starlette.sse import EventBusResponse

@app.post("/tools/call-stream")
async def call_tool_stream(req: ToolCallRequest):
    async def event_generator():
        yield {"event": "start", "data": json.dumps({"status": "processing"})}
        result = await registry.call(req.tool_id, req.params)
        yield {"event": "done", "data": json.dumps(result)}

    return EventBusResponse(event_generator())
```

---

#### 五、MCP 协议简化版（用官方 SDK）

```python
// server_with_sdk.py
# 用 mcp-sdk 实现（官方库）
from mcp.server import MCPServer
from mcp.tool import tool

server = MCPServer("demo-server")

@server.tool(name="get_weather", description="查询天气")
def get_weather(city: str) -> dict:
    """查天气工具"""
    return {"temp": 25, "weather": "晴"}

@server.tool(name="query_order", description="查订单")
def query_order(order_sn: str) -> dict:
    """查订单工具"""
    return {"status": "paid", "amount": 299.0}

server.run(host="0.0.0.0", port=8000)
```

```python
// client_with_sdk.py
from mcp.client import MCPClient

async def main():
    client = MCPClient("http://localhost:8000")

    # 自动发现所有工具
    tools = await client.list_tools()
    print(tools)

    # 调用工具
    result = await client.call_tool("get_weather", {"city": "福州"})
    print(result)

    await client.close()

asyncio.run(main())
```

---

#### 六、面试回答

> MCP（Model Context Protocol）让 LLM 能调用外部工具，核心是 Agent Card + 工具调用接口。
>
> **Agent Card**：`/.well-known/agent.json` 声明这个服务器有哪些工具、什么参数，LLM 自动发现，不需要人工配置。
>
> **工具调用**：`POST /tools/call` 传 `{tool_id, params}`，服务器执行后返回 `{success, data}` 或 `{success, false, error}`。
>
> **流式返回**：通过 SSE 边处理边返回，LLM 可以流式展示中间状态。
>
> 我项目里用 MCP 封装了天气查询、订单查询、邮件发送等工具，Agent 根据用户意图自动选择调用哪个。

---

### 13.24 RAG 常见面试问题

#### 13.24.1 RAG 和微调的区别，怎么选

| | RAG | 微调（Fine-tuning） |
|--|-----|------|
| **方式** | 检索外部知识，动态注入上下文 | 改模型权重，模型记住知识 |
| **成本** | 低，不用训练模型 | 高，需要 GPU 和训练数据 |
| **实时性** | 高，新文档即时生效 | 低，需要重新训练 |
| **幻觉** | 低，答案有据可查 | 中等，模型可能胡编 |
| **适用场景** | 知识库问答、实时性要求高 | 风格调优、特定任务 |

**选择建议**：
- 知识库更新频繁 → RAG
- 模型需要特定风格/领域适应 → 微调
- 两者结合效果最好：微调让模型更懂领域，RAG 提供实时知识

**我们的实践**：检索 + 生成，RAG 主流程；微调用来提升意图识别准确率。

---

#### 13.24.2 RAG 检索效果不好怎么排查

**排查思路**：定位是检索问题还是生成问题。

| 现象 | 问题 | 解决 |
|------|------|------|
| 答案里没出现正确信息 | 检索根本没召回 | 查召回率，扩召回量 |
| 答案里出现了但不对 | 检索召回对但排名差 | 调 Rerank，精排优先 |
| 答案完整但幻觉多 | 生成层问题 | 加 Faithfulness 约束 |
| 关键信息被截断 | Chunk 切分问题 | 语义切分，加 overlap |

**检索问题排查步骤**：
```
1. 查 Recall@K：正确答案有没有在 Top-K 里
   → 没召回：扩召回策略（加 BM25 / 混合检索）
   → 召回量够但排名低：加 Rerank

2. 查 MRR@K：正确答案排第几
   → 排名太烂：调相似度阈值，加重排序

3. 看 Bad Case：分析具体是哪个 Chunk 没召回对
```

---

#### 13.24.2.1 RAG 检索正确但生成错误，可能是什么原因？怎么改进？

**核心判断**：如果正确答案已经在 Top-K 检索结果里，但最终回答还是错，问题主要不在召回层，而在**上下文组织、Prompt 约束、生成模型理解、证据引用和后处理校验**这几层。

**先定位是不是“生成错误”**：

| 检查点 | 怎么看 | 结论 |
|------|------|------|
| Top-K 里是否包含正确 Chunk | 人工看 retrieved_docs | 有正确 Chunk 才能判断为生成侧问题 |
| 正确 Chunk 排名是否靠前 | 看正确 Chunk 是 Top1 还是 Top10 | 排名太低仍然可能是排序问题 |
| Prompt 里是否真的注入了正确 Chunk | 打印最终 prompt/context | 检索对但没拼进去，是工程问题 |
| 模型答案是否和证据矛盾 | 对比 answer 和 context | 矛盾就是 Faithfulness 问题 |
| 是否跨多个 Chunk 才能回答 | 看答案是否需要综合多段 | 可能是上下文整合能力不足 |

**常见原因**：

1. **正确 Chunk 排名低或被无关 Chunk 干扰**
   - Top-K 里有正确内容，但前面有很多相似但错误/过期内容，模型优先参考了错误证据。
   - 解决：加 Rerank，把 Top-50 粗召回后精排成 Top-5；过滤低置信度或过期文档。

2. **Chunk 切分不合理，证据不完整**
   - 正确片段只包含半句话，关键条件在上一个或下一个 Chunk，模型拿到的信息不完整。
   - 解决：语义切分、增加 overlap、按标题层级切分；返回相邻 Chunk；对表格、步骤类文档做结构化解析。

3. **上下文太长，模型没有关注到关键证据**
   - 检索结果太多，Prompt 里塞了大量上下文，正确答案被淹没。
   - 解决：控制 Top-K；按 rerank 分数排序；把关键 Chunk 放前面；对长上下文先做摘要或提取证据句。

4. **Prompt 约束不够，模型自由发挥**
   - Prompt 只说“根据知识库回答”，但没要求“只能基于证据”“无法确定就拒答”。
   - 解决：加硬约束：答案必须来自给定上下文；找不到依据就回答“暂时无法解答”；要求输出引用来源。

5. **问题需要多跳推理，模型没有正确综合**
   - 比如用户问“设备 A 报 E03 且水压低怎么办”，需要同时结合故障码、水压、设备型号多个片段。
   - 解决：Query Rewrite / Multi-query，把复杂问题拆成多个子问题检索；生成前先抽取证据，再基于证据合成答案。

6. **知识冲突或版本冲突**
   - 检索结果里同时有旧版手册和新版手册，模型选了旧答案。
   - 解决：metadata 过滤版本、时间、业务线；文档入库时加 version/effective_time；检索时优先最新有效文档。

7. **模型能力或参数设置问题**
   - 小模型理解复杂上下文能力弱；temperature 高会增加发散。
   - 解决：降低 temperature；复杂问题切换更强模型；对固定格式答案使用结构化输出。

**改进方案**：

| 层级 | 改进动作 |
|------|------|
| 检索后处理 | Rerank、去重、按版本/权限/时间过滤、低分拒答 |
| Context 组织 | 正确 Chunk 前置、相邻 Chunk 补全、标题和来源一起注入 |
| Prompt 约束 | 只能基于上下文回答、必须引用证据、无依据就拒答 |
| 生成策略 | 先抽证据再回答、复杂问题拆解、多轮验证 |
| 结果校验 | Faithfulness 检测、答案和证据一致性校验、敏感答案人工兜底 |
| 评估闭环 | 记录 bad case，区分 Retrieval Error / Generation Error，持续跑 RAGAS 指标 |

**可以直接这么回答面试官**：

> 我会先判断是不是检索层的问题：看正确答案是否进入 Top-K、排名是否靠前、最终 Prompt 里是否真的包含了这段上下文。如果正确 Chunk 已经进入 Prompt，但答案仍然错，那就是生成侧问题。
>
> 生成错的原因一般有几类：第一是正确证据排名低，被无关 Chunk 干扰；第二是 Chunk 切分导致上下文不完整；第三是 Prompt 约束不够，模型没有严格基于证据回答；第四是多跳问题需要综合多个片段，模型没有推理对；第五是知识库存在新旧版本冲突。
>
> 改进上，我会做几件事：检索后加 Rerank，把正确证据排到前面；优化 Chunk 切分和 overlap，必要时返回相邻 Chunk；Prompt 里明确要求“只能根据上下文回答，找不到就拒答，并给出引用来源”；对于复杂问题先做 Query Rewrite 或拆子问题检索；最后用 Faithfulness、Answer Relevance 这类指标和 bad case 集合持续评估。
>
> 所以 RAG 排查不能只看最终答案，要把链路拆开：检索有没有召回、排序是否合理、上下文是否注入、模型是否忠实于证据。这样才能判断到底该改检索，还是该改生成。

---

#### 13.24.3 混合检索是什么，为什么要用

**混合检索** = 向量检索 + 关键词检索，两者取并集或交集。

```
用户query
    ↓
┌───────────────┐     ┌────────────────┐
│ 向量检索      │     │ BM25 关键词检索  │
│ (语义相似)    │     │ (字面匹配)       │
└───────────────┘     └────────────────┘
         ↓                     ↓
         └──────── ┬ ────────────┘
                  ↓
            取 Top-K 并集
                  ↓
               Rerank
                  ↓
              生成答案
```

**为什么向量检索不够**：
- 用户问"怎么重置密码" → 向量搜"忘记密码"语义接近，但文档写的是"找回密码"
- 专业术语/缩写 → 向量不懂，但 BM25 能匹配

**我们的实践**：用 Milvus 存向量 + MySQL 存原始文本，查询时向量检索取 Top-50，BM25 取 Top-50，取并集后再 Rerank。

---

#### 13.24.4 Rerank 是什么，怎么做

**Rerank（重排序）**：用更精准的模型对粗排结果重新排序。

| 阶段 | 算法 | 速度 | 精度 |
|------|------|------|------|
| **粗排** | ANN（HNSW/IVF）| 快 | 一般 |
| **精排** | Cross-Encoder | 慢 | 高 |

**原理**：
- 粗排：ANN 算法按向量相似度快速召回 Top-K（如 50 条）
- 精排：用 Cross-Encoder 逐条计算 query 和 doc 的相关性得分，重新排序

**Cross-Encoder vs Bi-Encoder**：
```
Bi-Encoder：query 和 doc 分别编码 → 算余弦相似度 → 快但不精准
Cross-Encoder：query + doc 一起过模型 → 直接算相关性 → 慢但精准
```

**代码示例**：
```python
from sentence_transformers import CrossEncoder

# 用 Cross-Encoder 精排
model = CrossEncoder("BAAI/bge-reranker-v2-m3")

# 输入是 [(query, doc), ...] 对
pairs = [
    ("怎么重置密码", "密码找回操作指南"),
    ("怎么重置密码", "登录页面点击忘记密码"),
    ("怎么重置密码", "联系客服修改密码"),
]
scores = model.predict(pairs)
# [0.92, 0.78, 0.35] → 第一条最相关，排第一
```

**我们的实践**：粗排召回 Top-50 → Cross-Encoder 精排 → 取 Top-5 送生成。

---

### 13.25 Agent 常见面试问题

#### 13.25.0 你对 Harness Engineering 的理解

**回答**：
> 我理解 Harness Engineering，本质上就是给大语言模型搭一个可运行、可约束、可验证的工程框架，让模型从“会生成文本”变成“能稳定完成任务的 Agent”。
>
> 因为 LLM 本身有几个结构性问题，比如无状态、只能生成文字、上下文有限、输出有概率性，所以工程上要补很多层能力。
>
> 我一般会从五层理解：
> 1. **记忆层**，解决状态保存和跨轮连续性；
> 2. **执行层**，通过工具调用、代码执行、MCP 让模型真正能行动；
> 3. **反馈层**，通过结构化输出、校验、评估、重试来保证结果可靠；
> 4. **编排层**，负责任务拆解、多步骤流程和多 Agent 协作；
> 5. **沙箱层**，限制模型的执行边界，避免越权访问和高风险操作。
>
> 所以我觉得 Harness Engineering 不只是 Prompt 优化，而是在设计一个让模型能被稳定驾驭的系统。

**一句话版**：
> Prompt Engineering 更偏单点输入优化，Harness Engineering 更偏系统工程，重点是把模型接上记忆、工具、反馈、编排和安全边界。

#### 13.25.1 什么是 Agent，和 Chatbot 有什么区别

| | Chatbot | Agent |
|--------|-------|
| **交互模式** | 问答，打一仗算一仗 | 自主规划，多步执行 |
| **记忆** | 无状态或简单 Session | 持久化记忆，可累积 |
| **工具调用** | 人工触发 | 自动决定是否调用、调用哪个 |
| **目标导向** | 无 | 有目标，自动规划路径 |
| **执行过程** | 单轮响应 | 多轮协作，可回退 |

**Agent 核心能力**：
1. **规划（Planning）**：分解任务，制定执行步骤
2. **记忆（Memory）**：短期上下文 + 长期知识
3. **工具调用（Tool Use）**：按需调用外部 API / MCP

---

#### 13.25.1.1 常见智能体范式有哪些

面试里如果问“你理解哪些 Agent 范式”，可以从这三个讲起：`ReAct`、`Plan-and-Execute`、`Reflection`。

| 范式 | 核心思想 | 适用场景 | 优点 | 局限 |
|------|----------|----------|------|------|
| **ReAct** | 边推理边行动 | 搜索、问答、动态工具调用 | 灵活，适合不确定场景 | 容易绕圈，步骤不可控 |
| **Plan-and-Execute** | 先规划，再分步执行 | 复杂任务、长链路任务 | 结构清晰，便于拆解 | 计划一旦错，后面都受影响 |
| **Reflection** | 执行后自我检查再修正 | 高准确性任务、代码/文档/复杂回答 | 能发现错误并迭代修正 | 成本更高，延迟更高 |

**回答模板**：
> 我理解常见的智能体范式至少有三类。第一类是 ReAct，也就是边推理边行动，模型会在思考和工具调用之间交替进行；第二类是 Plan-and-Execute，先把任务拆成计划，再逐步执行；第三类是 Reflection，也就是执行完先做自我检查，发现问题后再修正。三者的区别主要在于，ReAct 更灵活，Plan-and-Execute 更结构化，Reflection 更强调结果质量。

---

#### 13.25.1.2 什么是 ReAct

**核心思路**：
> ReAct = Reasoning + Acting。也就是模型不是一次性给最终答案，而是先想一步、做一步，再根据工具返回结果继续想下一步。

**典型流程**：
```text
Thought -> Action -> Observation -> Thought -> Action -> Final Answer
```

**适合什么场景**：
- 需要实时查资料、查数据库、查地图、查服务站
- 用户问题不够完整，要边查边决定下一步
- 工具返回结果会影响后续动作

**优点**：
- 灵活，适合不确定性高的任务
- 能根据实时 observation 调整下一步动作
- 对工具型任务比较自然

**缺点**：
- 如果约束不够强，可能重复调用工具
- 推理链太长时，成本和延迟会升高
- 工程上可控性不如固定流程

**面试回答**：
> ReAct 的核心是边推理边执行。模型先做一步思考，再调用一个工具，根据工具返回结果继续思考和决策，直到完成任务。它很适合搜索、知识库问答、地图导航这类动态场景。像我的售后多智能体项目里，Agent 做服务站查询、知识库检索这类任务时，就很适合用 ReAct 思路，因为下一步动作往往取决于上一步查到的结果。

---

#### 13.25.1.3 什么是 Plan-and-Execute

**核心思路**：
> Plan-and-Execute 的重点不是边做边想，而是先把大任务拆成步骤，再按步骤执行。

**典型流程**：
```text
User Goal -> Planner 生成计划 -> Executor 按步骤执行 -> 汇总结果
```

**适合什么场景**：
- 任务比较复杂，步骤比较多
- 适合先拆解再执行的流程型任务
- 希望整体流程更可解释、更可控

**优点**：
- 任务结构清晰
- 便于多 Agent 分工
- 更适合长链路任务编排

**缺点**：
- 前面的计划如果拆错了，后面执行也会跟着偏
- 对动态变化的场景不如 ReAct 灵活
- 往往要额外处理“计划更新”问题

**面试回答**：
> Plan-and-Execute 更像项目管理思路，先规划后执行。比如用户需求比较复杂，模型先拆成几个子任务，再逐步完成。它适合复杂长任务，因为结构更清晰、工程上也更容易管理。如果我要把售后系统继续扩展成更复杂的工单流转、诊断、通知一整套流程，我会更倾向于用 Plan-and-Execute，而不是完全依赖单轮 ReAct。

---

#### 13.25.1.4 什么是 Reflection

**核心思路**：
> Reflection 可以理解为“先做，再回头检查”。Agent 先给出一个结果，然后再让自己检查结果有没有逻辑错误、事实错误、漏掉条件，必要时再修正一轮。

**典型流程**：
```text
Generate -> Review / Critique -> Revise -> Final Answer
```

**适合什么场景**：
- 对准确性要求高的回答
- 文档生成、代码生成、复杂分析
- 一次输出不够稳，需要二次校验

**优点**：
- 可以提升答案质量
- 能发现遗漏、矛盾和格式问题
- 对复杂任务尤其有效

**缺点**：
- 会增加 token 成本和响应时延
- 如果 review 标准不清晰，反思不一定有效
- 不是所有简单任务都值得多一轮反思

**面试回答**：
> Reflection 的核心是让 Agent 先产出结果，再做一轮自我审查。比如检查有没有答非所问、有没有和上下文冲突、有没有遗漏关键条件。如果发现问题，再修正输出。这个范式更适合高质量要求场景，比如代码生成、长文回答、复杂业务分析。它本质上是在用额外一次推理换更高的结果稳定性。

---

#### 13.25.1.5 ReAct、Plan-and-Execute、Reflection 怎么区分

**一个简洁回答**：
> 三者最大的区别在控制方式。ReAct 是边想边做，适合动态任务；Plan-and-Execute 是先规划后执行，适合复杂长任务；Reflection 是先产出再复查，适合高准确率要求任务。我的理解是，这三个不是互斥关系，实际项目里经常会组合使用，比如先做 Plan，再在每一步里用 ReAct，最后在输出前再加一层 Reflection。

**如果面试官问“你项目里更接近哪一种”**：
> 我的售后项目更接近 ReAct + Tool Use，因为很多动作是根据用户问题动态调用知识库、地图和其他工具；如果后续任务链再变复杂，比如加入工单诊断、审批、通知闭环，我会考虑往 Plan-and-Execute 方向扩展；如果对最终答案一致性要求更高，也可以在最终输出前补一层 Reflection 做结果校验。

---

#### 13.25.1.6 Claude Code 的记忆怎么实现的

**回答**：
> Claude Code 的记忆本质上不是模型自己永久记住了什么，而是靠 **上下文 + 文件 + 外部存储** 实现的。

**可以拆成三层讲**：
1. **短期记忆**：当前对话历史、当前任务说明、当前打开的代码，这些会一起放进上下文窗口里。
2. **中期记忆**：README、配置文件、Skill 文件、项目文档，这些不是一直塞在上下文里，而是需要时再读取。
3. **长期记忆**：如果要真正持久化，一般靠数据库、向量库、文件系统来存，再在下次任务里检索回来给模型。

**面试一句话版**：
> Claude Code 的记忆不是参数级记忆，而是工程化记忆。短期靠上下文窗口，中期靠代码和文档再读取，长期靠文件、数据库或向量库存储。

**如果面试官继续追问“和普通聊天记忆有什么区别”**：
> 普通聊天主要记对话内容，Claude Code 这类代码 Agent 还会把代码仓库、文档、Skill、配置一起当成记忆来源，所以它记住的不只是“你说过什么”，还包括“项目里有什么”。

---

#### 13.25.2 多 Agent 怎么协作

**协作模式**：
1. **层次链**：主 Agent 分发任务给子 Agent，汇总结果
2. **并行**：多个 Agent 同时执行任务，结果汇总
3. **循环**：Agent A 输出 → Agent B 输入 → Agent A 再处理

**示例：售后客服 Agent 系统**
```
用户：我的显示器坏了

协调Agent（主）
    ↓ 意图识别
    ├── 技术专家Agent：查知识库，判断故障类型 → "可能背光模组问题"
    ├── 订单Agent：查用户购买记录 → "2024年购，保修期内"
    ├── 服务站Agent：查附近服务点 → "福州万象城服务中心"
    ↓
协调Agent 汇总 → "您的显示器可能在保修期内，建议去福州万象城服务中心检修"
```

**代码示例（简化版）**：
```python
class CoordinatorAgent:
    def __init__(self):
        self.agents = {
            "tech": TechExpertAgent(),
            "order": OrderAgent(),
            "map": MapAgent(),
        }

    async def handle(self, user_query: str):
        # 1. 分解任务
        sub_tasks = await self.plan(user_query)  # ["查故障", "查订单", "查服务站"]

        # 2. 并行执行子任务
        results = await asyncio.gather(*[
            self.agents[task["agent"]].execute(task["query"])
            for task in sub_tasks
        ])

        # 3. 汇总结果
        return await self.summarize(user_query, results)
```

---

#### 13.25.3 什么是 ReAct 模式

**ReAct** = Reasoning + Acting，让 Agent 边想边做。

**原理**：
```
用户问题 → Think（思考下一步）→ Act（执行工具）→ Observe（观察结果）
                                                     ↓
                                          ← 循环直到完成
```

**示例**：
```
用户：帮我查下我上个月买的显示器发货了没

Thought：我需要先查用户的订单，找到订单后再查物流状态。
Action：call_tool("query_order", {"user_id": "U123", "month": "2025-04"})
Observe：{"order_sn": "ORD20250401", "status": "shipped", "tracking": "SF123456"}
Thought：找到了订单状态是已发货，物流单号是SF123456，再查一下物流详情。
Action：call_tool("query_tracking", {"tracking_no": "SF123456"})
Observe：{"status": "在途", "eta": "2025-05-10"}
Answer：您的显示器已于5月5日发货，预计5月10日送达。
```

**LangChain 已有 ReAct 实现**：
```python
from langchain.agents import AgentType, initialize_agent
from langchain.agents.agent_toolkits.conversational import create_conversational_react_agent

agent = create_conversational_react_agent(
    llm, tools, prompt
)
# Agent 自动边推理边调用工具
```

---

#### 13.25.4 什么是 LangGraph，和 LangChain 的区别

**LangChain** 是链式调用，线性执行，适合简单流程。

**LangGraph** 是图结构，支持循环、条件分支，适合复杂 Agent。

```
LangChain（链）：
A → B → C → D

LangGraph（图）：
    A
  ↙   ↘
B ←→ C
```

**适合用 LangGraph 的场景**：
- Agent 要循环执行（直到某个条件满足）
- 有条件分支（根据结果走不同路径）
- 多个 Agent 互相调用
- 需要人工审核节点（Human-in-the-loop）

**代码示例**：
```python
from langgraph.graph import StateGraph, END
from typing import TypedDict

class AgentState(TypedDict):
    user_query: str
    intent: str
    result: dict

# 建图
graph = StateGraph(AgentState)
graph.add_node("router", router_node)      # 意图识别
graph.add_node("tech", tech_agent)         # 技术Agent
graph.add_node("order", order_agent)       # 订单Agent
graph.add_node("respond", respond_node)    # 汇总响应

# 定义边
graph.add_edge("router", "tech", condition=lambda s: s["intent"] == "tech")
graph.add_edge("router", "order", condition=lambda s: s["intent"] == "order")
graph.add_edge("tech", "respond")
graph.add_edge("order", "respond")
graph.add_edge("respond", END)

app = graph.compile()
# 执行
result = await app.ainvoke({"user_query": "我的订单到哪了"})
```

---

#### 13.25.5 什么是 Human-in-the-Loop（HITL）

**HITL**：Agent 在关键步骤停下来等人工确认，再继续执行。

**典型场景**：
- 涉及金钱操作（退款、修改订单金额）
- 涉及用户隐私（查身份证号、查详细地址）
- 敏感操作（删除数据、取消订单）

**代码示例**：
```python
class HITLAgent:
    async def handle(self, user_query: str):
        plan = await self.plan(user_query)  # 规划步骤

        for step in plan["steps"]:
            if step.requires_approval:
                # 发消息给人工审批队列
                task_id = await self.send_to_approval(step)
                # 等待人工响应（异步阻塞）
                approved = await self.wait_for_approval(task_id)
                if not approved:
                    return {"status": "rejected", "reason": "人工否决"}

            # 执行步骤
            result = await self.execute(step)

        return result
```

**我们的项目里**：服务站查询、邮件发送等敏感操作前会触发人工审批，用户确认后才真正执行。

## 14 transformer原理

“Transformer 本质上是一种基于注意力机制的模型，它最核心的思路就是：在处理一句话的时候，不再像 RNN 那样按顺序一个一个词往后传，而是让每个词都能直接去看整句话里跟自己最相关的部分，所以它更擅长捕捉长距离依赖，也更方便并行计算。

它的整体流程可以简单理解成几步。第一步是把输入文本先变成向量，也就是词向量。因为 Transformer 本身没有循环结构，不知道词的前后顺序，所以还会额外加一个位置编码，让模型知道谁在前谁在后。

第二步就是最关键的自注意力机制。可以把每个词映射成三个向量，分别叫 Query、Key 和 Value。然后用当前词的 Query 去和其他词的 Key 做匹配，算出相关性分数，再经过 softmax 变成权重，最后对所有词的 Value 做加权求和。这样模型就能动态决定，当前这个词在理解时应该更关注哪些词。

在这个基础上，Transformer 又用了多头注意力。我的理解就是，不只用一种方式看句子，而是让模型从多个角度同时去关注信息。比如有的头关注语法关系，有的头关注语义关系，这样表达能力会更强。

注意力之后，通常还会接一个前馈神经网络，对每个位置的表示再做一次非线性变换。整个过程中还会配合残差连接和层归一化，这样训练会更稳定，网络也能堆得更深。

如果从结构上讲，经典 Transformer 分为编码器和解码器两部分。编码器负责理解输入，解码器负责一步一步生成输出。像机器翻译里，编码器先把原句编码好，解码器再根据前面已经生成的内容和编码结果继续往下生成。 

